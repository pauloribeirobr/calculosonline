# Features — Calculos Online

Lista numerada de funcionalidades, por ordem cronológica. Cada item recebe um
identificador estável (`F1`, `F2`, ...) para ser referenciado em conversas,
commits e PRs futuros — "faz parte do F12" em vez de reexplicar o contexto.
Mesmo padrão do projeto irmão [Recibo Fácil](../recibofacil/FEATURES.md).

- **Fonte:** cruzamento de `git log` (commits desde 10/05/2026) com
  [`CHANGELOG.md`](CHANGELOG.md) (versionado desde 19/07/2026, mais granular
  para o período recente), [`AGENTS.md`](AGENTS.md) (sprints do plano de
  implementação) e [`MEMORY.md`](MEMORY.md) (decisões/contexto de growth).
- **Granularidade:** feature/entrega, não commit — vários commits da mesma
  sprint viram um único item quando pertencem à mesma entrega.
- **Datas:** data do commit principal (ou do último commit da entrega),
  formato `AAAA-MM-DD`.
- Este arquivo é **complementar** ao `CHANGELOG.md` (detalhe técnico e
  versionamento SemVer por app) e ao `MEMORY.md` (narrativa e decisões
  completas). Aqui o foco é só "que feature, quando, qual número".

---

## 1. Melhorias desenvolvidas (histórico)

| # | Data | Feature |
|---|------|---------|
| F1 | 2026-05-10 | Setup do monorepo — Turborepo + pnpm workspaces, pacotes `@calculosonline/core` e `@calculosonline/ui` (Sprint 0.1) |
| F2 | 2026-05-10 | Core engine — funções de cálculo TypeScript puras em `packages/core/src/` (`trabalhista`, `impostos`, `financeiro`, `investimentos`, `saude`, `negocios`), cobertura Vitest (Sprint 0.2) |
| F3 | 2026-05-10 | 20 calculadoras (formulários + lógica) — registry canônico `calculatorRegistry` em `apps/web/src/lib/calculators.ts` (Sprint 1.1) |
| F4 | 2026-05-10 | Componentes UI compartilhados — `CalculatorLayout`, `CalculatorForm`, `CalculatorResult`, `Breadcrumb`, `AdSlot`, `LegalBadge`, `RelatedCalculators`, `UpdatedBadge` em `@calculosonline/ui` (Sprint 1.2) |
| F5 | 2026-05-10 | Páginas web — home, `/calculadora/[slug]` (SSG+ISR), `/categorias`/`/categoria/[categoria]`, `/sobre`, `/contato`, políticas; `Header`/`Footer`/`PageSeo`/`JsonLd` globais (Sprint 1.3) |
| F6 | 2026-05-10 | SEO + conteúdo editorial — 20 MDX em `content/calculadoras/[slug].mdx`, `ContentLoader`, MDX no `next.config.ts`, `next-sitemap`, rota IndexNow (Sprint 1.4) |
| F7 | 2026-05-10 | Memória de cálculo + identidade visual — `explainability/`, `MemoriaCalculo`/`PassoCalculo`, tipografia numérica (`tabular-nums`), `CalculatorIcon.tsx`, hero com stack de cards (Sprint 1.4.1) |
| F8 | 2026-05-11 | Deploy 1.0.0 (produção) + ajuste de geração do sitemap |
| F9 | 2026-05-11 | Google Analytics |
| F10 | 2026-07-19 | Correções de SEO (diagnóstico GSC 17/07) — `buildCalculatorTitle()`, `FAQPage` real via `lib/faq.ts` (101 perguntas/20 calculadoras) — **v0.1.0** |
| F11 | 2026-07-19 | Playwright E2E (17 testes) + sitemap/robots nativos (`app/sitemap.ts`/`app/robots.ts`, substitui `next-sitemap`) + fix crítico: campo numérico opcional vazio virava `NaN` e travava submissão em quase todas as 20 calculadoras — **v0.2.0** |
| F12 | 2026-07-20 | Fix do `lastmod` do sitemap (`seoRefreshDate`) + schema `HowTo` + chips de valor rápido (`quickAdd`, 16 formulários/17 calculadoras) — **v0.3.0** |
| F13 | 2026-07-20 | Ajuste de CI |
| F25 | 2026-07-25 | Ativação do Microsoft Clarity em produção (Project ID real) + alinhamento com o padrão do Recibo Fácil (`lazyOnload`, preconnect) — **v0.3.1** |
| F27 | 2026-07-25 | Retargeting de SEO da calculadora de Décimo Terceiro — title/H1/FAQ passam a incluir "décimo terceiro" por extenso (antes só "13º"), mesmo padrão do "recibo-simples" no Recibo Fácil — **v0.3.2** |
| F28 | 2026-07-25 | Valores padrão automáticos nos 19 formulários (derivados do `.default()` do próprio schema Zod, fix centralizado em `CalculatorForm`) + navbar simplificada (removidos links duplicados/redundantes "Categorias"/"Blog"/"Sobre"/"Contato" do menu superior) — **v0.4.0** |
| F29 | 2026-07-26 | Detalhamento do cálculo sempre visível (sem toggle) + sinal (+)/(−) em cada linha do detalhamento, além da cor — **v0.4.1** |
| F30 | 2026-07-26 | Fix: `pnpm dev` caindo sozinho após ~1min30 — `turbo.json` `"ui": "tui"` → `"ui": "stream"` (bug conhecido do Turborepo 2.x com tarefas persistentes) — **v0.4.2** |
| F31 | 2026-07-26 | Valor padrão 0 também em campos obrigatórios (reverte decisão da F28/0.4.0, 24 campos/16 formulários) + mês de admissão do 13º com nome do mês (`<select>` numérico, 1º do projeto) — **v0.4.3** |
| F32 | 2026-07-27 | Compartilhamento de cálculo por link (WhatsApp) nas 20 calculadoras — query param `?d=` em base64, reabre repreenchido e já recalculado, sem exigir clique — **v0.5.0** |
| F16 | 2026-07-27 | `llms.txt` + GEO — `apps/web/public/llms.txt`, mesmo padrão do Recibo Fácil (20 calculadoras por categoria, modelo de negócio, diferenciais, FAQ curta) — **v0.6.0** |
| F33 | 2026-07-27 | Fix crítico no 13º salário: 2ª parcela (e 1ª) calculava meses de direito usando o mês real do calendário do sistema em vez de 12 meses fixos para quem já trabalhava antes do ano corrente — resultado errado e dependente da data do cálculo. Corrigido em `packages/core/src/trabalhista/decimo-terceiro.ts` + suíte de 24 testes — **v0.6.1** |
| F21 | 2026-08-05 | Centralização de SEO — `buildMetadata()` central em `lib/seo.ts` (canonical alternates, OpenGraph/Twitter consistentes) adotado nas 7 páginas com `Metadata` manual; `JsonLd.tsx` ganha `HowToJsonLd`/`ItemListJsonLd`/`ArticleJsonLd` (paridade com o Recibo Fácil); `BreadcrumbList` das 20 calculadoras migrado para o `BreadcrumbJsonLd` com `@id`; 6 páginas institucionais que não tinham JSON-LD nenhum ganharam `WebPage`+`BreadcrumbList` — **v0.7.0** |
| F34 | 2026-08-05 | **Piloto de UX em formulário — campo de moeda com centavos, stepper e listas itemizadas** (calculadora de Salário Líquido). 3 tipos de campo novos e reutilizáveis no `CalculatorForm` compartilhado (`type: 'currency'`, `'stepper'`, `'itemList'`) + suporte a itens `{descrição, valor}` no motor de cálculo (`ItemValor`, `packages/core/src/types.ts`). Corrige de quebra um bug real: "outras deduções" (plano de saúde, PGBL) só abatia a base do IRRF, sem reduzir o salário líquido de fato. Testes novos incluem o 1º projeto mobile do Playwright (`mobile-chromium`, toque real via `tap()`). Ver F35 no backlog — replicar o padrão nas outras 19 calculadoras — **v0.8.0** |
| F35 | 2026-08-06 | **Replicado o padrão do F34 nas outras 18 calculadoras.** Campos monetários → `currency`; contagens inteiras pequenas (dependentes, dias, prazo em meses, idade) → `stepper` (que ganhou `max` opcional, simétrico ao `min`, usado em férias vencidas 0-2 e dias de abono ≤10). `itemList` só entrou no IRRF ("outras deduções") — único outro campo com o mesmo formato de "bucket" genérico; IRPF tem categorias de dedução legalmente distintas, não viraram lista. Novo helper `apps/web/src/lib/itemListField.ts` reaproveitado pelo salário líquido (refatoração, sem mudar comportamento). Decisão consciente de **não** mexer em campos decimais (horas extras, taxas, markup) nem em `PorcentagemForm`/`IMCForm`. **v0.9.0** |
| F36 | 2026-08-09 | **3º modo da margem de lucro + correções de SEO on-page saídas do export do GSC de 09/08.** (1) `calcularMargemLucro` ganhou `margemDesejadaPercent` — informar custo + margem desejada e obter o preço de venda (`Preço = Custo ÷ (1 − Margem)`), a intenção de maior volume do cluster (`calcular preço de venda`, 480 buscas/mês no Semrush) que a calculadora simplesmente não atendia; era também a query de maior impressão identificada no GSC (`calcular valor baseado na margem de lucro`, posição 66,6 contra 21,5 da query cujo modo já existia). (2) `descricao` da porcentagem estava truncada ("...percentual de e mais") e ia assim pra SERP — reescrita. (3) Nova flag `atemporal` no registry tira o "2026" do title de cálculos que não dependem de tabela anual (porcentagem, margem de lucro). (4) **H1 duplicado eliminado nas 20 calculadoras** — todo MDX abria com `#`, que virava um segundo `<h1>` competindo com o do shell. **v0.10.0** |
| F37 | 2026-08-13 | **Histórico local de cálculos (IndexedDB) nas 20 calculadoras.** Inspirado no módulo `history`/`LocalStorageRepository` do Recibo Fácil, mas mais simples (sem `Blob`, registro é só JSON pequeno) e sem seleção prévia de quais calculadoras participam — todas ganham o botão "Salvar cálculo" de saída, o usuário decide por cálculo. Reaproveita a serialização do F32 (compartilhamento por link): reabrir um cálculo salvo é a mesma URL `?d=`. Nova página `/meus-calculos` (listar/abrir/remover, `noindex`) e link novo no Header. **v0.11.0** |
| F38 | 2026-08-20 | **Vocabulário "simulador/simulação" nas 5 calculadoras financeiras** (`tesouro-direto`, `financiamento`, `poupanca`, `cdb`, `emprestimo`). Saiu do export do GSC de 20/08 cruzado com o Semrush: o cluster somava 180 impressões em 97 queries (posição média 83,3) e `simulação tesouro direto` tem **2.9K buscas/mês** — a maior keyword do site por volume — mas a palavra não aparecia em nenhum title, H1, MDX ou FAQ. Campo `sinonimos?: string[]` novo no registry; `tituloLongo` vira "Calculadora **e Simulador** de X" (muda `<title>` e `<h1>` juntos); descrições reescritas nas formas nominais; H2 + parágrafo novo e 1 pergunta nova de FAQ em cada um dos 5 MDX. `buildCalculatorTitle` ganhou orçamento de 78 caracteres — o USP "sem Cadastro" sai quando não cabe (custa 14 caracteres e rendeu 4 impressões em 3 meses), o que de quebra corrigiu o pior title do site (`decimo-terceiro`, 90 → 76). Mesmo padrão do F27, com alvo maior — **v0.12.0** |
| F39 | 2026-08-20 | **Vocabulário de demissão/acerto trabalhista na calculadora de rescisão.** O cluster `demissão / acerto trabalhista / verbas / desligamento` somava 116 impressões em 84 queries com **posição média 93,7 — a pior do site** — enquanto o motor já cobria todas essas modalidades desde o F3 (`MotivoRescisao`). Gap de vocabulário puro: "acerto" aparecia 0 vez no MDX. Descrições reescritas, `sinonimos` com as 6 formas populares, seção nova "Cálculo por motivo de saída da empresa" (um H3 por modalidade, incluindo rescisão indireta) e 5 perguntas novas de FAQ — a página foi de 6 para 11 no `FAQPage`. Corrigiu de quebra uma afirmação factual errada: o MDX dizia em dois pontos que no acordo mútuo o FGTS "pode ser sacado integralmente", quando o art. 484-A, §1º limita a 80% — **v0.13.0** |
| F40 | 2026-08-20 | **As duas leituras do aviso prévio no acordo mútuo, mostradas lado a lado.** Saiu de uma divergência encontrada ao escrever o conteúdo do F39: o art. 484-A, I manda pagar "metade do aviso prévio" sem dizer metade de quê — metade do mínimo de 30 dias (15 fixos, o que o motor fazia) ou metade do aviso proporcional da Lei 12.506/2011 (cresce 3 dias/ano). Coincidem até 1 ano de casa; aos 10 anos divergem em R$ 1.500 no líquido para um salário de R$ 3.000. Paulo decidiu **mostrar as duas** em vez de escolher. O corpo do cálculo virou `montarRescisao(diasAvisoPrevio)` e roda duas vezes no acordo mútuo — trocar os dias de aviso reprojeta férias, 13º, INSS e IRRF, então a 2ª leitura é um cálculo completo, não uma estimativa da linha do aviso. Resultado principal inalterado (leitura restritiva, a mais praticada); a 2ª entra como linhas `neutro` fora da soma. Também corrigiu de vez a afirmação errada sobre saque de FGTS no acordo (80%, não integral) herdada do F39 — **v0.14.0** |
| F41 | 2026-08-20 | **Identidade visual unificada por categoria + ícone em todas as superfícies.** O `CalculatorIcon` do F7 já era bom; faltava alcance e faltava uma fonte de verdade. As 6 categorias eram descritas em **4 tabelas paralelas** (2 de label, 2 de cor em formatos incompatíveis, já divergentes) — agora tudo sai de `lib/identidadeVisual.ts`, com tokens atômicos e sem importar React (é lido por `sitemap.ts`/`schema.ts`). Ícone e texto viraram tokens separados por contraste medido: `-600` no glifo (objeto gráfico, piso 3:1) e `-700` no texto (piso 4,5:1, que o `-600` reprova em âmbar/esmeralda/ciano). Suavização: ponto decorativo removido, glifo de `-700`→`-600`, `ring` padronizado. E o ícone chegou onde não existia — **as 20 páginas de calculadora** (onde cai todo o tráfego orgânico), os cards de relacionadas e `/meus-calculos` — via slot `ReactNode`, já que `packages/ui` não importa de `apps/web` — **v0.15.0** |
| F42 | 2026-08-20 | **Imagem social (og-image) gerada por calculadora, a partir do ícone e da cor da categoria.** Corrige um 404 que estava em produção desde sempre — `seo.ts` apontava toda página para `/images/og-image.png`, que nunca existiu, quebrando o preview de todo link do F32 no WhatsApp. 20 imagens + a do site, geradas no build via `next/og` com a identidade do F41. Três obstáculos que valem memória: o Satori não renderiza `forwardRef` (os Heroicons somem sem erro — resolvido desembrulhando o `.render()` em `glifoHeroicon`), `div` com 2 nós filhos exige `display` explícito, e o App Router tem duas armadilhas opostas — `openGraph.images` sobrescreve a convenção de arquivo, e `openGraph` sem `images` suprime a herança do pai (era por isso que `/sobre` e `/categorias` não tinham imagem alguma) — **v0.16.0** |

| F43 | 2026-08-27 | **Escultura de link interno — `relacionadas`, rodapé e links contextuais nos MDX.** O export do GSC de 27/08 mostrou 31 links internos **idênticos** para todas as páginas (o `Footer.tsx` listava as 20 calculadoras em todo lugar: quando tudo linka tudo, nenhum link significa nada) e o único diferencial real, o bloco `relacionadas` do registry, estava **invertido** — `financiamento`, maior página do site (298 impressões), recebia **1** link; `juros-compostos` (3 impressões, pos. 97,7) recebia **8**. Com Authority Score 2 e um único backlink reconhecido, o PageRank interno é o único capital de autoridade 100% sob controle. Três frentes: (1) as 20 listas de `relacionadas` reescritas — `financiamento` 1→6, `fgts` 2→6, `hora-extra` 2→5, enquanto `juros-compostos` 8→4 e `salario-liquido` 7→3; as duas páginas com backlink externo real (`salario-liquido`/band.com.br e `decimo-terceiro`/acritica.com) passaram a apontar **para** os alvos do P0, porque são fonte de autoridade, não destino; (2) rodapé reduzido de 20 links para 8 destaques + as 6 categorias + "Todas as calculadoras", com `/categorias` virando o hub; (3) **20 links contextuais dentro dos MDX, onde antes havia zero** — todo link interno vinha do rodapé ou do bloco `relacionadas`, ambos fora do corpo do texto, que é justamente onde o anchor text carrega contexto — **v0.17.0** |
| F44 | 2026-08-27 | **URLs quebradas com tráfego real.** O Clarity (25-27/08, 25 sessões) mostrou `https://calculosonline.com.br/site` respondendo por **4 sessões — 16% do total** — e `/2026/calculadora/cdb` por 1; nenhuma das duas existe em `app/` e o `next.config.ts` não tinha bloco `redirects`. A origem é externa (link errado em diretório/agregador), então só o redirect resolve. **Achado maior, encontrado ao ler o código e não nos dados:** o `Footer.tsx` linkava `/blog` — rota que **nunca existiu** — em **todas as páginas do site**, ou seja, um 404 interno que o Google rastreava em 100% dos crawls, desperdiçando exatamente o PageRank que o F43 redistribui. Bloco `redirects` novo (`/site`, `/site/*` e prefixo de ano espúrio `/:ano(\d{4})/calculadora/:slug`) + link morto removido do rodapé — **v0.18.0** |
| F45 | 2026-08-27 | **Evento de conversão do GA4 documentado (passo de painel, não de código).** `calculator_calculated` já era disparado corretamente por `analytics.calculatorCalculated()` (385 eventos em 28 dias no export anterior) — o que faltava era marcá-lo como *key event* no painel do GA4, coisa que a API de coleta não faz. Sem isso o GA4 reporta "Leads qualificados 0" em todas as semanas desde janeiro e **o Google Ads não tem o que otimizar**, que é o motivo real de o F17 estar bloqueado. Procedimento completo (caminho no painel, o que marcar, e as duas ressalvas — o GA4 não retroage, e o evento só aparece na lista se foi coletado nas últimas 48h) na seção "Operação" do `README.md`. ✅ **Executado pelo Paulo em 2026-09-10** — `calculator_calculated` está marcado como evento principal e com streaming ativo no painel. Como o GA4 não retroage, "Leads qualificados" só deixa de ser 0 para as semanas a partir desta data; destrava o F17 e a leitura do `calculator_edited` do F59. **Correção de 22/09:** "Leads qualificados" conta o evento recomendado `qualify_lead`, que o site não dispara, e **vai continuar 0**. O evento principal aparece como **"Eventos principais"**: 235 no export de 25/08→21/09. O F45 está funcionando |
| F46 | 2026-08-27 | **IndexNow automático depois do deploy de produção.** Desde 20/08 o script existia (`pnpm --filter web indexnow`) mas **nada o disparava** — sem hook, cron ou build step — e ele precisa rodar *depois* do deploy, senão os buscadores rastreiam o conteúdo antigo. Com o Bing respondendo por ~95% do tráfego real (462 sessões contra 4 do Google no GA4 de vida inteira), era o passo de maior retorno e o mais fácil de esquecer. Workflow `.github/workflows/indexnow.yml` escutando `deployment_status`, filtrado por `state == 'success'` **e** `environment == 'Production'` (o evento dispara em todo estado e também em Preview), mais `workflow_dispatch` com campo de slugs para submissão manual. Sem `pnpm install` nem build: o script não tem dependências e lê a lista de URLs do `sitemap.xml` de produção. Exige o secret `INTERNAL_API_KEY` no GitHub, mesmo valor da Vercel — **criado pelo Paulo em 2026-08-30; o workflow está armado** |

| F47 | 2026-08-27 | **Tabelas de referência numéricas nas 5 páginas de maior impressão** (`hora-extra`, `ferias`, `fgts`, `poupanca`, `financiamento`). Saiu da comparação com a SERP: quem rankeia publica "quanto vale sua hora extra se você ganha R$ 2.000" numa tabela, enquanto o site só respondia **depois** de a pessoa preencher o formulário — e nem o Google nem o modelo que cita a página preenchem formulário. 8 tabelas novas ao todo, incluindo conversão de minutos (hora extra), parcela por prazo (financiamento) e multa de 40% projetada (FGTS). **Todos os números foram gerados rodando o próprio core** (script descartável em `packages/core/src/__scratch__`, removido depois), não escritos à mão — se uma regra mudar, a tabela e a calculadora não divergem, e `tests/e2e/tabelas-referencia.spec.ts` quebra — **v0.19.0** |
| F48 | 2026-08-27 | **Hora extra: adicional livre, minutos, DSR — e a correção do adicional noturno.** 240 impressões e posição 84,7 no GSC de 27/08; o único clique do site na lista de queries veio de "calculo hora extra". O concorrente que rankeia tem H2 separado para 50%/60%/70%/100%, aceita minutos e calcula DSR; esta página tinha um `select` de quatro opções fixas. Entregue: **(1)** adicional de CCT/ACT selecionável (60/70/80/100%) ou percentual livre, com o piso legal preservado; **(2)** campo de minutos — cartão de ponto marca "1h30", não "1,5"; **(3)** reflexo no DSR (Lei 605/49, Súmula 172 do TST) com dias úteis e dias de repouso configuráveis; **(4)** hora noturna reduzida do art. 73 §1º, opcional porque depende de o usuário ter horas de relógio ou horas já convertidas. **Bug real corrigido:** o motor cobrava 1,50× na hora noturna enquanto o MDX da mesma página afirmava 1,80× (1,20 × 1,50) — o adicional noturno do art. 73 simplesmente não era aplicado, e o conteúdo é que estava certo. Core de 16 para 32 testes — **v0.20.0** |
| F49 | 2026-08-27 | **Exemplos resolvidos com números fechados nas 5 páginas do F47.** 14 casos nomeados ("Marina, auxiliar administrativa, 44h/semana, R$ 2.000, 12 horas extras…") com o cálculo inteiro escrito. É o formato que `calculadorabrasil.com.br` usa e que a IA generativa cita — e o site tem tração de citação comprovada (295 citações em 7 dias no Clarity). **Cada exemplo foi conferido rodando o core**, não estimado: incluem o caso de faltas que derrubam os dias de férias (CLT art. 130), o abono pecuniário isento, o acordo mútuo com saque de FGTS limitado a 80%, a CCT de 70% e a hora noturna com e sem redução — **v0.21.0** |
| F50 | 2026-08-27 | **Chips de valor rápido nos campos `stepper`.** Terceira confirmação independente no Clarity: o heatmap do `cdb` (25-27/08) tem 4 pageviews e **133 cliques, dos quais 64 (48%) num único campo** (`prazoMeses`) — sair de 12 para 24 meses custa 12 cliques no +/−. As duas janelas anteriores foram `ferias` em 09/08 e em 20/08. O bloco de chips do F12 já renderizava para qualquer campo; o que faltava era o comportamento correto em stepper: o chip agora **respeita `min`/`max`** (senão "+24" estouraria o teto e reprovaria no Zod) e **"Zerar" volta ao mínimo, não a zero** (em campo com `min: 1` zerar deixava o formulário inválido de propósito). Presets novos (`QUICK_ADD_MESES`, `QUICK_ADD_DIAS`, `QUICK_ADD_HORAS`, `QUICK_ADD_IDADE`) aplicados a 9 formulários — só em faixa larga; contadores curtos (dependentes, férias vencidas 0-2) seguem sem chip, onde seriam ruído — **v0.22.0** |
| F51 | 2026-08-27 | **Campo de data mascarado + atalhos, no lugar do seletor nativo.** Maior atrito medido do site: **11 dos 24 cliques** da página de rescisão no heatmap do Clarity foram nos dois campos de data, e `input[type=date]` é ruim no Edge/Windows — 61% do público (GA4, vida inteira). `DateField` novo em `packages/ui`: digita-se `01011990` e lê-se `01/01/1990`, com máscara progressiva e `inputMode=numeric`. **O valor guardado continua sendo ISO (`YYYY-MM-DD`)**, o mesmo que o `type=date` produzia, para não mexer no core nem no link compartilhado do F32. Ganho de correção junto: data inexistente (31/02) não vira ISO e é reprovada pelo Zod, em vez de o `new Date` "consertar" para 03/03 silenciosamente. Atalhos ao lado do campo via `dateShortcuts` ("Hoje", "Fim do mês", "Há 1/2/5/10 anos"). **Escopo menor que o previsto:** o plano falava em rescisão, férias e 13º, mas só a rescisão tem campo de data — férias e 13º trabalham com meses e dias — **v0.23.0** |

| F57 | 2026-08-27 | **Fix de precisão nas férias: valor diário arredondado antes de multiplicar.** `calcularFerias` fazia `arredondar(salarioBruto / 30) * diasGozados`, então o erro de centavos do valor diário era multiplicado pelos dias — um salário de **R$ 2.000 rendia R$ 2.000,10** de férias (66,67 × 30) e R$ 2.500 rendia R$ 2.499,90. Aparece em qualquer salário não divisível por 30. Achado ao gerar as tabelas do F47 a partir do próprio motor. `decimo-terceiro.ts` e `rescisao.ts` **sempre fizeram certo** (`arredondar((salario / 12) * meses)`) — férias era a única do módulo trabalhista fora do padrão. A suíte não pegava porque **todos os casos existentes usavam salário divisível por 30**, então os testes novos usam de propósito salários que não são. Duas invariantes travadas junto: férias de 30 dias equivalem exatamente ao salário do mês, e **o detalhamento exibido sempre soma o total exibido** — esta última é a razão de os componentes seguirem arredondados um a um em vez de saírem de um arredondamento único no fim, e implica um centavo residual de diferença ao vender dias, que é correto e agora está documentado no teste. Tabela de férias do F47 refeita com salários redondos, já que a limitação que a forçava a usar múltiplos de 30 deixou de existir — **v0.23.1** |

| F52 | 2026-08-27 | **Painel Share of Authority do Clarity virou etapa fixa do ciclo de análise.** É a única fonte de keyword research do projeto com ciclo de **7 dias** — o Search Console leva 90, porque a posição média que ele reporta é média da janela inteira e não move com um deploy recente. Já pagou o próprio custo uma vez: o F38 apostou em "simulador/simulação" pelo volume do Semrush, as citações do tesouro triplicaram em 7 dias (15 → 47) **mas nas variações de "calculadora de investimento em tesouro direto"**, que o F38 não mirou — sem o painel, a conclusão registrada teria sido a errada. Procedimento na seção "Operação" do `README.md`: como ler, como separar **vocabulário** (a página faz, só não chama assim → F53) de **lacuna de produto** (a página não faz → F54), e as duas armadilhas — "SoA 24%" quer dizer "24% no Copilot", não "24% na IA", e citação não é clique |
| F53 | 2026-08-27 | **Vocabulário do painel de IA aplicado a 7 calculadoras.** Mesmo tratamento do F38/F39 (registry + descrição + MDX + FAQ), mas agora com vocabulário **medido por citação real**, não por volume de busca. `sinonimos` novos em `irrf`, `irpf`, `inss`, `ferias`, `calorias` e acrescentados em `tesouro-direto` e `cdb` — o site foi de 6 para 11 calculadoras com vocabulário alternativo declarado. Seções novas no corpo do texto, que é onde o Google e o modelo leem (o `keywords` não ranqueia nada desde 2009): "Como calcular o IR na folha de pagamento" e "Cálculo do dependente no IR" no IRRF, "Vale a pena investir no Tesouro Direto?" no tesouro e "Como calcular o INSS online em 2026" no INSS. **A correção de rumo que o dado impôs:** o tesouro recebeu o vocabulário de "investimento", que foi o que de fato gerou as 47 citações, e não mais o de "simulação" — **v0.24.0** |
| F54 | 2026-08-27 | **IRRF sobre aluguel recebido.** `calculadora irrf 2026 aluguel` e `calculadora irrf aluguel 2026` apareceram no painel do Clarity com 4 citações cada — demanda comprovada que a calculadora não atendia. Modo novo dentro do formulário existente (mesmo padrão do 3º modo do `margem-lucro` no F36), não página nova. Mesma tabela progressiva mensal; **muda o que se abate antes dela**: aluguel não sofre INSS (a contribuição incide sobre rendimento do trabalho, e locação é rendimento de capital), e entram IPTU, condomínio e taxa de administração quando o ônus é do locador (RIR/2018 art. 42; IN RFB 1.500 art. 31). Benfeitoria e reforma **não** entram — são custo do imóvel, e isso está dito no conteúdo porque é o erro mais comum. Dependentes e pensão seguem valendo. `fonteJuridica` e `baseCalculo` do resultado mudam com a origem. Conteúdo novo explicando quem recolhe (PJ retém na fonte; PF vira carnê-leão) + 4 perguntas de FAQ. Suíte do IRRF de 13 para 22 testes — **v0.25.0** |
| F55 | 2026-08-27 | **JSON-LD `Dataset` nas tabelas legislativas (GEO).** O `irrf` é a página nº1 do site em citação por IA (158 de 295 em 7 dias) e ao mesmo tempo invisível no Google (88 impressões, pos. 81) — `Dataset` é o vocabulário que declara "esta página publica uma tabela de dados com vigência e fonte", que é o que um modelo precisa para citar a versão certa e não uma tabela de 2024 achada em outro lugar. Campo `dataset?` novo no registry (nome, descrição, `vigenciaInicio`, `variaveis`) e componente `DatasetJsonLd` em `JsonLd.tsx`, com `temporalCoverage` no formato aberto `2026-01-01/..` ("vigente desde, sem fim conhecido"), `dateModified` real e `citation` apontando para a fonte jurídica. Aplicado a `irrf`, `inss`, `irpf` e `das-mei` — as quatro que publicam tabela de verdade; calculadora que só aplica fórmula (`porcentagem`) **não** declara, e há teste travando isso, porque declarar ali seria ruído para o modelo. **Ressalva registrada no código:** isto não gera rich result — `Dataset` só aparece no Google Dataset Search, não na SERP normal. O valor é de GEO, não de CTR — **v0.26.0** |

| F56 | 2026-08-29 | **O celular: 9 de 20 páginas rolavam na horizontal, e a causa era uma linha de config.** O GSC de 27/08 mostrou 765 impressões no celular em pos. 54,6 contra 1.413 no desktop em pos. 71 — o Google rankeia o site **16 posições melhor no celular** — e zero cliques. **A hipótese de performance caiu na primeira medição:** Lighthouse mobile 94/100/75/100, LCP 2,9 s, CLS 0,002. Não é lentidão, é render. Auditoria de viewport nas 22 rotas (Pixel 7, build de produção) achou **9 páginas com rolagem horizontal** — `financiamento` 606px numa viewport de 412, `das-mei` 514, `fgts` 508, `poupanca` 462, `rescisao` 448, `cdb` 434, `emprestimo` 432, `calorias` e `salario-liquido` 418 — sempre a tabela do MDX. **Causa raiz: `mdx-components.tsx` mora na raiz de `apps/web` (exigência do App Router) e estava fora de todos os globs de `content` do Tailwind, então nenhuma classe daquele arquivo era gerada** — `overflow-x-auto`, `min-w-full`, `border-collapse`, `scroll-mt-20`, `border-l-4`, `border-brand-500` e `italic` conferidas uma a uma como ausentes do CSS. O wrapper rolável da tabela estava escrito certo desde sempre e nunca funcionou: as tabelas do F47 nasceram desktop-only. **2º achado:** todos os campos eram 14px e o iOS dá zoom automático abaixo de 16px — **iOS é 27 dos 50 usuários mobile**. Agora `text-base md:text-sm`. Mais: wrapper de tabela virou `role="region"` alcançável por teclado, e os "Ver todas" de `/categorias` foram a 24px (piso WCAG 2.2). Suíte `viewport-mobile.mobile.spec.ts` nova trava as duas invariantes nas 20 calculadoras — **v0.27.0** |

| F22 | 2026-08-30 | **Blog sazonal — infra completa + o guia do 13º.** Maior lacuna do plano de negócios (seção 3.2), nunca implementada; até o F44 o rodapé linkava `/blog`, rota que **nunca existiu**, em 100% das páginas. Registry canônico (`lib/blog.ts`) no padrão do `calculators.ts`, MDX em `content/blog/`, hub `/blog` + `/blog/[slug]` em SSG/ISR 24h, `ArticleJsonLd`/`Breadcrumb`/`ItemList`, sitemap com `lastModified` do hub derivado do post mais recente, e **og-image por post** herdando ícone e cor da calculadora que ele alimenta (F42/F41 — o blog não inventa paleta própria). **Escolhido pelo calendário:** a busca por 13º pica em nov/dez e o `MEMORY.md` (25/07) já registrava que perder a janela adia o retorno em um ano, não em semanas — publicar em agosto dá 60-90 dias de maturação. O gancho do artigo é que **20/12/2026 cai num domingo**, antecipando a 2ª parcela para 18/12. **Números todos gerados pelo `calcularDecimoTerceiro`** (disciplina do F47/F49): 3 tabelas, 4 exemplos nomeados e o degrau dos 15 dias, em que um dia de admissão vale R$ 246,81; 9 valores travados em e2e. **Link recíproco (F43)** nos dois sentidos, e o bloco `GuiasRelacionados` **não renderiza** nas 19 calculadoras sem post. **Corrigiu de quebra um bug que afetava as 20 calculadoras:** o `rehype-autolink-headings` com `behavior: 'wrap'` + a regra `.prose a` pintavam **todo H2 dos MDX de azul sublinhado**, com cara de link. E o teste do F44 ("o rodapé não linka /blog") foi substituído pela invariante durável — **nenhum link do rodapé pode dar 404** — **v0.28.0** |

| F58 | 2026-09-01 | **Hub "Calculadora Trabalhista Completa" — o fluxo único que encadeia rescisão + 13º + férias + FGTS.** Último item de P1 que não dependia de decisão do Paulo. O GSC mostra dezenas de queries de intenção agregada (`calculo trabalhista completo`, `como calcular direitos trabalhistas`) que **nenhuma das 10 calculadoras trabalhistas atende sozinha** — cada uma responde um pedaço —, e o único candidato que o site tinha para elas era `/categoria/trabalhista`, um índice de links com **1 pageview em 3 meses**. Rota nova `/calculadora-trabalhista-completa`, com um formulário de 7 campos (os mesmos da rescisão, nada a mais: o hub tem de custar *menos* preenchimento que abrir quatro calculadoras) e quatro blocos de resultado, cada um com detalhamento linha a linha e link para a calculadora dedicada. **O encadeamento é função pura no core** (`calcularPanoramaTrabalhista`, 19 testes Vitest), não lógica de página — reaproveitável por desktop/Sheets/API e testável sem browser. **A armadilha que o módulo existe para não cair: os quatro números não se somam.** A rescisão já embute 13º proporcional, férias proporcionais e a multa do FGTS; a soma ingênua dá **R$ 36.681,33** contra os **R$ 15.558,57** que a pessoa realmente recebe (salário R$ 3.000, 5 anos) — mais que o dobro. Por isso não há "total geral", o consolidado sai **inteiro como `neutro`** (a UI só desenha sinal em crédito/débito) e o aviso aparece **acima** dos números, não em rodapé. Segunda armadilha fechada: o saldo do FGTS que o usuário digita já contém os depósitos do contrato, então a projeção roda com `saldoAtual: 0` — passá-lo somaria os mesmos 8% duas vezes; o bloco compara os dois e sinaliza depósito faltando. **Não entrou no `calculatorRegistry`** de propósito (mudaria a contagem de "20 calculadoras" declarada em home/`/sobre`/FAQ/og-image e o colocaria concorrendo com as que agrega); mora em `lib/hubTrabalhista.ts`. **Link recíproco (F43)** das quatro calculadoras encadeadas + `/categoria/trabalhista` + rodapé — sem isso nasceria órfão. 3 tabelas e 5 valores de FAQ **gerados rodando o próprio motor** (F47/F49), travados em e2e; og-image própria com os quatro ícones — **v0.29.0** |

| F59 | 2026-09-01 | **Abrir cálculo salvo/compartilhado já no resultado, com botão "Editar cálculo".** Atrito notado pelo Paulo: abrir um cálculo em `/meus-calculos` ou por link compartilhado (F32/F37) leva a `/calculadora/[slug]?d=…&calc=…`, o `autoSubmit` calcula — e a viewport ficava no topo. A pessoa pedia um número e recebia o formulário de novo, com o resultado escondido abaixo da dobra. Agora a abertura leva **scroll e foco** até a região do resultado, e um botão "Editar cálculo" volta ao formulário. **A metade que costuma faltar é o foco:** rolar sozinho move a tela e deixa o cursor de teclado/leitor de tela parado no topo — a região do resultado e o formulário viraram alvos `tabIndex={-1}` (focáveis por código, fora da ordem de Tab) e o `preventScroll` no `.focus()` evita o segundo pulo do navegador. Respeita `prefers-reduced-motion` no `behavior` do scroll. **Três limites deliberados:** (1) só com `?d=`/`?calc=` na URL — em visita normal a página abre no H1, senão o tráfego orgânico, que é quase todo o tráfego, pularia o conteúdo do F47 e a estrutura de links do F43; (2) dispara **uma vez só**, no primeiro resultado auto-calculado, para um recálculo manual não rolar de novo e não brigar com a restauração de scroll do botão "voltar"; (3) o botão "Editar cálculo" **não** aparece em cálculo feito na hora, onde o formulário está logo acima, na mesma tela. Evento `calculator_edited` novo, para responder com dado — e não com palpite — se o botão deveria aparecer em toda abertura com resultado. **45 testes e2e novos cobrindo as 20 páginas**, e não uma amostra: o comportamento é único (`CalculadoraPageClient`), mas a altura do formulário vai de 2 campos (IMC) a 11 (hora extra), e é nas longas que a regressão passaria despercebida. O fixture de entrada por calculadora tem trava de cobertura contra o registry, e já pagou por si: pegou que `calorias` mede altura em **centímetros** enquanto `imc` mede em **metros** — **v0.30.0** |

| F19* | 2026-09-08 | **Conta do AdSense verificada (parte 1 do F19; a exibição continua desligada).** Paulo criou a conta e passou o Publisher ID. Entregue só o que a verificação de posse exige: meta `google-adsense-account` no `<head>` (via `metadata.other` do layout raiz, conferida no HTML gerado das 20 calculadoras, do hub e da home) e o `ads.txt` real, saindo do placeholder `pub-XXXX`. **O Publisher ID é constante em `lib/seo.ts`, não env** — mesmo motivo da chave do IndexNow: é público por design e o `ads.txt` é estático, então env criaria duas fontes de verdade com falha silenciosa. `NEXT_PUBLIC_ADSENSE_PUBLISHER_ID` mudou de papel e passou a ser a chave que **liga a exibição** no `AdSlot`; vazia até a aprovação, e com ela vazia nenhum script do `adsbygoogle` carrega. `adsense.spec.ts` trava a invariante que machuca: **meta e `ads.txt` não podem divergir** — a falha clássica em que a verificação passa e o `ads.txt` é marcado inválido semanas depois, sem nada no site acusar. **Nenhum `AdSlot` foi posicionado em página**: onde colocar anúncio segue sendo decisão do Paulo — **v0.30.1** |
| F60 | 2026-09-08 | **4 posts do cluster 13º/rescisão, dentro da janela sazonal.** O export do GSC de 08/09 mostrou `rescisao-trabalhista` indo de 138 a **266 impressões (+93%)** e `decimo-terceiro` de 130 a **239 (+84%)** — as duas páginas que mais cresceram são as do pico de nov/dez, e estão em posição **91,5** e **85,8**. Cada post mira uma query do próprio export: acordo mútuo (`calculo rescisão comum acordo`, pos. 94,8), acerto trabalhista (`acerto trabalhista`, pos. 89,3), 13º proporcional (`calcular 13º proporcional online`) e direitos na demissão (`calculadora trabalhista`, pos. 91,9 — a query que alimenta o F58). Custaram o que o F22 prometeu: 4 entradas em `lib/blog.ts` + 4 `.mdx`; rota, SEO, `Article`, og-image, sitemap e links recíprocos saíram de graça. **Campo `ctaHub` novo:** o post de intenção agregada manda o CTA para o hub do F58, não para uma calculadora — quem pergunta "quais são todos os meus direitos" precisa das quatro contas. `calculadoraPrincipal` continua preenchida (é ela que dá a identidade da og-image), e `postsDoHub()` faz o hub apontar de volta, senão o post nasceria órfão (F43). **Cada artigo foi construído em torno de uma armadilha, e a do agregado é a do próprio F58: a soma é o bug** — abre com os R$ 36.681,33 da soma ingênua contra os R$ 15.558,57 reais, com teste de `boundingBox().y` travando o aviso acima da tabela. **Todos os números saíram do core** (script descartável, removido): 11 tabelas, 9 exemplos nomeados, 23 valores travados em e2e — **v0.31.0** |

| F61 | 2026-09-08 | **FGTS com headline por modalidade + `hojeISO()` + chips que definem valor.** Registrado retroativamente em 10/09 — subiu no commit `b61fec7` junto com a cauda do F60 e por isso **divide a versão 0.31.0** com ele. (1) O headline do FGTS era `depositoMensal` sempre, a única das 20 calculadoras cujo número grande não respondia à pergunta do formulário — quem preenchia 24 meses via o valor de um mês; agora segue a modalidade, via campo `rotuloResultado` novo em `ResultadoCalculo`. (2) **`new Date().toISOString()` é UTC e o servidor roda em UTC**, então a partir das 21h de Brasília o rótulo "Tabelas:" exibia a data de amanhã — flagrado no FGTS às 22h de 08/09 mostrando `2026-09-09`. `hojeISO()` novo em `utils` (via `en-CA`, cujo formato curto já é `AAAA-MM-DD`), aplicado a 11 módulos do core; no FGTS a data virou a constante `VIGENCIA_REGRAS_FGTS = '2020-01-01'`, porque FGTS não tem tabela anual. (3) "Saldo Projetado" → "Saldo acumulado (sem rendimento)", com aviso sobre TR + 3% a.a. (art. 13 da Lei 8.036/1990) — a linha prometia projeção que não acontece e, com saldo zerado, ficava idêntica à de cima. (4) Chips ganham `modo: 'definir'` e `titulo`, chegando a campos de taxa, peso, altura e aporte: somar é certo para dinheiro e errado para percentual, porque dois cliques em "+1%" dariam 2%. 10 presets novos, com as armadilhas de unidade (decimal × ponto percentual; cm no `calorias` × m no `imc`) documentadas no arquivo — **v0.31.0** |
| F62 | 2026-09-10 | **Relatório de erro do GA4 legível — falha de recurso sai de `exception` e `fatal` volta a significar "a pessoa perdeu a página".** 286 `exception` para 323 `page_view` no export de 08/09. A suspeita registrada (o `capture: true` recolhendo erro de recurso) **se confirmou, mas a atribuição estava trocada**: erro de recurso sempre foi `fatal: false`; quem marcava fatal era o `unhandledrejection`. Eram duas misturas independentes, nenhuma desfazível no painel depois. Falha de `<script>`/`<link>`/`<img>`/mídia virou evento próprio `resource_error` (o suspeito nº 1 é adblock derrubando Clarity e gtag); `unhandledrejection` e recuperação de chunk passam `fatal: false` por campo novo em `ErrorInfo`, com os error boundaries seguindo fatais; `exception` nunca mais sai sem `description` (erro cross-origin chega com `message` vazia e era a linha em branco do painel); e dedupe por assinatura + teto de 10 eventos por sessão, porque proporção de ~1:1 com pageview é o mesmo erro repetindo. `error-logger.spec.ts` novo com stub de `window.gtag`. **Não adivinha a causa — faz o próximo export dizer qual é** — **v0.31.1** |

| F63 | 2026-09-10 | **Aposentadoria deixa de ser um apelido de "sem justa causa" na rescisão.** A opção existia no formulário desde o F3 devolvendo resultado **idêntico** ao da demissão sem justa causa — multa de 40% e aviso prévio integral —, e a premissa não se sustenta: o STF derrubou os §§ 1º e 2º do art. 453 da CLT (ADI 1.721 e 1.770) e a **OJ 361 da SDI-1 do TST** fixou que a aposentadoria espontânea **não extingue o contrato**. Quem encerra é a empresa (→ dispensa, multa sobre todo o contrato) ou o próprio trabalhador (→ saída a pedido, sem multa). Com R$ 3.000 de salário, R$ 9.000 de FGTS e 3 anos de casa, a página afirmava **R$ 11.310,64** onde o correto na saída a pedido é **R$ 2.692,91** — R$ 8.617,73 de diferença. O motivo passou a calcular como pedido de demissão (sem multa, sem aviso indenizado, mantendo saldo, férias e 13º), e **o outro cenário não sumiu: entra como linha `neutro` fora da soma** ("Multa de 40% — só se o desligamento partir da empresa"), na mesma disciplina do F40 e do F58. Três `avisos` declaram a premissa assumida, mandam quem foi dispensado escolher "sem justa causa" e desfazem a confusão que provavelmente originou o bug — **a aposentadoria libera o saque do FGTS (art. 20, III) mesmo sem multa; sacar o saldo e receber os 40% são coisas diferentes**. O rótulo do `select` passou a dizer "Aposentadoria (saída a pedido do trabalhador)" nas duas superfícies (calculadora e hub do F58, que herda os avisos), e o MDX ganhou a seção que nunca existiu — a opção era oferecida no formulário e não era explicada em lugar nenhum — mais 2 perguntas de FAQ. Core de 408 para 413 testes + `rescisao-aposentadoria.spec.ts` (5 e2e) — **v0.31.2** |
| F64 | 2026-09-13 | **As tabelas de 2026 e o redutor da Lei 15.270/2025 — o motor estava calculando 2025 e devolvendo IRRF que a lei não cobra mais.** Saiu da avaliação de uma copy que o Paulo trouxe para a página do 13º: revisando os dois pontos que ela pedia para confirmar, a auditoria achou um defeito maior que a copy. **Três lacunas, todas no caminho do número final:** (1) `TABELAS_2026.inss` trazia a tabela de **2025** (`1.518 / 2.793,88 / 4.190,83 / 8.157,41`) rotulada como 2026, com `salarioMinimo: 1518` e o comentário ainda citando "Decreto a ser publicado em dezembro/2025" — o certo é `1.621 / 2.902,84 / 4.354,27 / 8.475,55` (Portaria Interministerial MPS/MF nº 13, de 09/01/2026, teto de R$ 8.475,55 e contribuição máxima de R$ 988,09); (2) o **redutor do IRPF da Lei 15.270/2025** não existia em lugar nenhum do código — `grep` por `15.270`, `redutor` e `978,62` não retornava nada, oito meses depois de a lei entrar em vigor; (3) o **desconto simplificado mensal de R$ 607,20** (art. 4º, IX da Lei 9.250/1995, vigente desde 2023) também nunca foi implementado — `calcularIRRFMensal` fazia `bruto − INSS − dependentes` e parava aí. **As três se somam num erro visível:** um 13º de R$ 3.000 saía com R$ 23,83 de IRRF e um de R$ 5.000 com R$ 334,85, quando a lei de 2026 cobra **zero** nos dois casos — e isso na página que carrega o único backlink do cluster do 13º (`acritica.com`, F15). **O ponto que decidiu a implementação:** a fórmula do redutor recebe o rendimento tributável **bruto**, não a base já líquida de INSS. Não é questão de interpretação — em R$ 5.000 ela devolve exatamente os R$ 312,89 do redutor máximo e em R$ 7.350 devolve exatamente zero; com a base após INSS seria descontínua num extremo e não zeraria no outro. Os dois extremos estão travados em teste como a prova. **E o desconto simplificado é o que faz a isenção fechar:** os R$ 312,89 são precisamente `(5.000 − 607,20) × 22,5% − 675,49`. Sem ele, um 13º de R$ 5.000 sobraria com **R$ 23,78** de imposto e a página prometeria uma isenção que a calculadora não entregaria. **Validação contra a fonte:** os cinco exemplos oficiais da Receita Federal ("Exemplos de Aplicação da Lei 15.270/2025") viraram teste e batem casa por casa — base, imposto apurado, redutor e imposto final. **Dois bugs de precisão achados no caminho, ambos de centavo e ambos visíveis:** `arredondar()` devolvia R$ 121,57 para `1.621 × 7,5%` porque `121.575 * 100` dá `121.57499...` em float e o `Math.round` derrubava o meio centavo — a tabela oficial publica R$ 121,58; e o INSS era arredondado **faixa por faixa**, somando quatro erros e fechando o teto em R$ 988,10 contra os R$ 988,09 da portaria. Agora o arredondamento normaliza a escala antes do round e o INSS arredonda o acumulado, distribuindo a diferença nas linhas — **as linhas exibidas continuam somando exatamente o total exibido**, invariante do F57. `TETO_INSS_2026` (constante hardcoded que duplicava a tabela e era a razão de o teto ter ficado em 2025) virou `getTetoINSS()`, derivado da tabela vigente. **Alcance:** 9 calculadoras mudaram de resultado (`decimo-terceiro`, `salario-liquido`, `inss`, `irrf`, `ferias`, `fgts`, `hora-extra`, `das-mei`, `rescisao-trabalhista`) e o hub do F58. **O conteúdo publicado estava mentindo junto, e foi todo regerado pelo motor** (disciplina F47/F49): 11 MDX de calculadora/hub e 5 posts do blog, incluindo a tabela de referência do 13º, a do salário líquido, a do acerto trabalhista (IRRF de 4 colunas foi a zero) e os R$ 36.681,33 da soma ingênua do F58, que agora são R$ 36.929,06. **Um erro preexistente caiu de tabela:** o hub afirmava multa de 40% e aviso prévio na linha de **Aposentadoria**, o que o F63 já havia corrigido no motor três dias antes — o texto tinha ficado atrás. **Links oficiais nas páginas (E-E-A-T), a pedido do Paulo:** base legal do 13º, do IRRF, do INSS e dos posts agora linkam a página de exemplos da Receita e a do teto do INSS no `gov.br`, as duas verificadas respondendo 200. As normas do Planalto ficaram **nomeadas sem link** de propósito: o domínio não respondeu deste ambiente e link não verificado é pior que nenhum. **Interlink do cluster fechado (F43):** o 13º passou a linkar `salario-liquido`, `inss` e `irrf`, que faltavam, e os três passaram a linkar de volta — `hora-extra` continua na lista porque a escultura de link do F43 depende desse inbound. Core de 413 para 438 testes; 9 e2e reancorados nos valores novos — **v0.32.0** |
| F65 | 2026-09-22 | **"Salário depois das férias" — o guia que responde a maior pergunta do relatório do Bing, e 7 correções de conteúdo achadas na apuração.** Pauta saída de dado: o relatório de consultas do BWT (08→20/09, o primeiro que veio) trouxe como maior pergunta em linguagem natural *"vou tirar 10 dias de férias somente, quanto receberia no proximo mês pós ferias"* (17 impressões, posição 6,8). `ferias` é a **4ª página do site no Bing** (292 impressões, posição 8,1) e o **3º tema em citações de IA**, com o menor Share of Authority dos temas grandes (14,5%) — e o `ferias.mdx` não tinha uma linha sobre o assunto. O post explica o mecanismo (os dias de férias saem da folha porque já foram pagos adiantados no recibo — CLT art. 145 × art. 459, §1º), com linha do tempo dos três pagamentos, tabela líquida por dias de férias (10/15/20/30 e 20+10 vendidos), tabela líquida por salário (R$ 1.621 a R$ 5.000), o efeito do abono, os descontos (INSS sobre o mês inteiro, IRRF em separado) e as regras de fracionamento. **Números todos do motor** (`calcularFerias`, `calcularINSSProgressivo`, `calcularIRRF`, `calcularSalarioLiquido`), com 10 valores travados em `blog-salario-pos-ferias.spec.ts` — disciplina do F22/F47/F60. Um achado do cálculo virou seção: em 2026 o salário de R$ 4.000 não paga IRRF, mas **30 dias de férias do mesmo salário pagam R$ 122,45**, porque o terço leva o recibo a R$ 5.333,33 e tira o valor da faixa onde o redutor da Lei 15.270/2025 zera o imposto; vendendo 10 dias, volta a zero. **As correções:** na `ferias.mdx`, (1) e (2) a promessa de "acréscimo de 50%" por atraso no pagamento, em dois lugares, citando a **Súmula 450 do TST** — que nunca falou em 50% e **foi declarada inconstitucional pelo STF na ADPF 501**; (3) a lista de "férias em dobro" citava o **art. 138 da CLT**, que trata de outro assunto, e inventava uma terceira hipótese — trocada pela regra do art. 137 + **Súmula 81 do TST**; (4) as férias do doméstico atribuídas à EC 72/2013 em vez da **LC 150/2015, art. 17**; (5) férias no aviso prévio justificadas pela **Súmula 261 do TST**, que trata de outra coisa; (6) o exemplo da venda de 10 dias dizia render "o mesmo total", ignorando que os dias vendidos são trabalhados e pagos na folha. Fora do MDX: (7) a calculadora de férias prometia "descontos de INSS e IRRF" em `descricaoCurta`, meta description e `llms.txt`, e `calcularFerias` **devolve bruto** — os três textos passaram a dizer o que a ferramenta faz; e `decimo-terceiro.mdx` atribuía o 13º do doméstico à EC 72/2013, quando vem da CF/88 (art. 7º, parágrafo único). Corrigido também um teste que estava **quebrado em silêncio desde o F64**: `blog.spec.ts` travava `dateModified` na data literal de 30/08 e o F64 moveu a data para 13/09 — agora a asserção sai do registry — **v0.33.0** |
| F66 | 2026-09-22 | **A calculadora de férias passa a devolver o líquido, com INSS e IRRF.** Fecha a lacuna que o F65 achou horas antes: `calcularFerias` devolvia só o bruto, enquanto o registry, a meta description e o `llms.txt` prometiam "descontos de INSS e IRRF" — e **"quanto vou receber de férias", que é pergunta de líquido, já era sinônimo declarado da calculadora**. A escolha é de canal: `ferias` é a **4ª página do site no Bing** (292 impressões, posição 8,1) e o 3º tema em citação de IA, com o menor Share of Authority dos temas grandes (14,5%). **Reaproveitar `calcularSalarioLiquido` não servia:** o recibo de férias tem três regras próprias, e são elas que entraram no motor — (1) o **IRRF é calculado em separado** do salário do mês, sobre a própria remuneração de férias, com a tabela progressiva e o redutor da Lei 15.270/2025; (2) o **abono pecuniário fica fora da base** de INSS e de IRRF (Lei 8.212/1991, art. 28, §9º); (3) a **dobra do art. 137 também fica**, porque indeniza o atraso na concessão em vez de remunerar trabalho — ponto em que sistemas de folha divergem, e por isso sai declarado em `avisos`. `resultado` vira o líquido (com `rotuloResultado` "Total líquido das férias") e `FeriasResultado` ganha `baseTributavel`, `descontoINSS`, `descontoIRRF`, `totalLiquido`, `irrfSemRedutor`, `redutorIRRF` e `usouDescontoSimplificado`; o formulário ganha stepper de dependentes. **O hub do F58 continua em bruto de propósito** — ali as férias são projeção de um período futuro, não recibo a pagar, e a legenda já declarava isso. Conteúdo regerado pelo motor: tabela de referência com colunas de INSS, IRRF e líquido, exemplos nomeados fechando no líquido e duas perguntas novas na FAQ. **O caso que virou argumento editorial:** R$ 5.100 vendendo 10 dias dá o mesmo bruto de quem goza os 30 (R$ 6.800), mas **R$ 6.363,82 líquidos contra R$ 5.365,67 — R$ 998,15 a mais só pela isenção do abono**. 8 testes novos no core e `ferias-liquido.spec.ts` no e2e; a invariante do F57 (linhas exibidas somam o total exibido) foi reescrita para os dois totais. **Bônus achado pelo e2e:** o rótulo das faixas do INSS saía **"Até R$ 1621.00"** (`toFixed(2)`, ponto decimal, sem separador de milhar) em toda calculadora que lista faixas — salário líquido, rescisão, 13º, INSS e férias —; agora usa `formatarBRL`. Nenhum teste lia esse rótulo — **v0.34.0** |
| F67 | 2026-09-22 | **Vocabulário da cauda média em hora extra e 13º — o alvo onde três medições dizem que dá para ganhar.** Molde do F38/F39/F53 (registry + title + copy), com alvo escolhido por dado: o Semrush de 21/09 põe o site em **pos. 54** em `calculo hora extra online` (390/mês, **KD 18**) e **58** em `calcular hora extra online` (720/mês, KD 25), contra 85-95 da cabeça do cluster — e o `calculadora.com.br`, com **AS 14**, está em **pos. 5** nessa mesma keyword e em 10 em `calculador hora extra`, sendo essa a **única página trabalhista que rankeia para ele**. No 13º o padrão é a **forma curta**, sem o "terceiro": `calcular meu decimo` (pos. 52, KD 24) e `calculo décimo` (pos. 57, 480/mês, KD 20). **Entregue:** `hora-extra` com `tituloLongo` "Calculadora de Hora Extra Online" (o `buildCalculatorTitle` corta o "sem Cadastro" sozinho se estourar os 78 caracteres), descrição com noturna/DSR e **7 sinônimos medidos**; `decimo-terceiro` com **5 sinônimos** da forma curta; e — porque `keywords` no `<head>` não ranqueia desde 2009 — o vocabulário **na copy**: parágrafo de abertura da hora extra e **3 perguntas novas na FAQ** ("Como calcular hora extra online?", "Qual é o cálculo exato de horas extras?", "Como calcular hora extra noturna?"), mais "Como calcular meu décimo?" no 13º. A resposta da noturna documenta o campo do F48 que quase ninguém entende (hora de relógio do ponto × hora já convertida do holerite). `vocabulario-cauda-media.spec.ts` trava title, H1, copy e registry. **Correção achada no caminho:** a copy dizia "com o adicional due", inglês vazado — **v0.35.0** |
| F69 | 2026-09-22 | **Calculadora de férias + 13º — a 21ª calculadora, e a primeira de intenção combinada.** Menor KD já medido no projeto: o Semrush de 21/09 põe o concorrente que tem essa página em **posição 1-3** numa família de ~18 keywords de **KD 11-24** (`calcular decimo terceiro e ferias` 390/mês KD 15; `calculo de 13 e ferias` 110/13; `calculadora de 13 e ferias` 90/**11**), porque **nenhuma calculadora de item único responde "vou tirar férias e receber o 13º, quanto entra no total"**. Entrou dentro da janela sazonal — o 13º pica em nov/dez e o Bing responde em semanas. **`calcularFeriasDecimoTerceiro` compõe as duas funções que já existiam** (F66 para o líquido das férias; `calcularDecimoTerceiro` para as parcelas) e devolve as três linhas reais: recibo de férias líquido, 1ª parcela (50%, sem desconto) e 2ª parcela (o que sobra depois do INSS/IRRF do 13º integral). **A armadilha é o espelho da do F58:** lá o erro é somar o que já está contido, aqui é achar que o salário do mês entra — ele vem menor por causa do adiantamento das férias (F65). O resultado avisa explicitamente que **não** inclui o contracheque, e um teste trava a frase; outro aviso registra que as duas contas são tributadas **em separado**. A opção de **adiantar a 1ª parcela junto das férias** (Lei 4.749/1965, art. 2º, §2º) leva o recibo de R$ 3.631,40 a R$ 5.131,40 num salário de R$ 3.000 **sem mudar o total do ano**. Conteúdo com tabela por salário, linha do tempo dos três pagamentos (2ª parcela antecipada para **sexta, 18/12/2026**, porque dia 20 é domingo), efeito da venda de 10 dias (+R$ 152,91) e 6 perguntas na FAQ, tudo do motor. 11 testes no core (456 no total) + `ferias-decimo-terceiro.spec.ts`; links recíprocos com `ferias` e `decimo-terceiro` no registry e no corpo dos MDX (F43) — **v0.36.0** |

> F19 aparece duas vezes de propósito: a linha acima é a **parte 1** (verificação), e o item F19 do backlog continua aberto para a decisão de exibição e do gate. É a primeira feature do projeto entregue pela metade por dependência de terceiro (aprovação do AdSense).

> F25, F27, F28, F29, F30, F31, F32, F33, F34, F35, F36, F37, F38, F39, F40, F41 e F42 saíram de ordem (implementados direto, fora do backlog planejado em F14-F24) e por isso ganharam número novo em sequência em vez de reutilizar um número já reservado — mesmo critério usado no Recibo Fácil para features implementadas fora da fila. F16 manteve o número original porque já estava reservado nesse backlog (P0, GEO/`llms.txt`), só foi implementado fora da ordem relativa a F15. F26 está reservado no backlog (Fase 2, ainda não implementado).

## 2. Próximas melhorias (backlog)

Numeração segue direto de onde a Parte 1 parou (pulando F25, já usado pelo
Clarity) — **a próxima feature nova implementada é a F14.** Fonte:
[`MEMORY.md`](MEMORY.md) (§Backlog ativo) e `AGENTS.md`/
`docs/PLANO_IMPLEMENTACAO.md` (fases do roadmap). Prioridade dentro de cada
grupo segue a ordem do `MEMORY.md`. **A próxima feature nova é a F65** (F22, F56, F57, F58, F59, F60, F61, F62, F63 e F64 já saíram) — os
números F43-F56 foram atribuídos em 2026-08-27 aos Blocos A-D do plano de
tráfego (ver `MEMORY.md`, diário 2026-08-27, §7), e F57 saiu no mesmo dia como
fix de precisão. **Do Bloco D, F56 saiu em 29/08 e F22 em 30/08; sobram F15
(backlinks) e F19, este agora *pela metade* — a conta do AdSense foi verificada
em 08/09 (v0.30.1) e o que resta é a decisão de exibição e do gate.**
**O F58 (01/09) fechou o último item de P1 que não dependia de decisão do
Paulo**, e o **F60 (08/09) executou a pauta de conteúdo saída do export de
08/09**. Daqui em diante o backlog implementável é o que sair da análise do
próximo export ("avalie a pasta gsc") — com uma ressalva de método que o export
de 08/09 deixou explícita: **o ciclo lê o console do buscador que manda zero
tráfego.** O Bing responde por ~60% dos usuários (98 de 177 em 28 dias) e o
Google por **0 sessões**, e o Bing Webmaster Tools não aparecia em lugar nenhum
do repositório — só o IndexNow, que fala com o Bing mas não escuta.
✅ **Site verificado no BWT pelo Paulo em 2026-09-10.** O painel não traz
histórico retroativo, então o primeiro export com dado utilizável fica para o
fim de setembro; a partir daí o ciclo "avalie a pasta gsc" passa a ler os dois
consoles, e o do Bing é o que tem tráfego real para explicar. **Reordenado em 2026-07-25** para trazer
aquisição de tráfego (backlinks + GEO/llms.txt + Google Ads) antes do
AdSense — e **refinado no mesmo dia** depois de cruzar o export novo do GSC
(25/07) com o GA4: o problema não é "site sem tráfego" (há ~230
usuários/mês reais via Bing/Direct/IA), é especificamente o Google não
mandando tráfego — ver `MEMORY.md` §P0 e diário de 2026-07-25 (parte 4).

### P0 — Aquisição de tráfego no Google (antes do AdSense)

| # | Feature | Contexto |
|---|---------|----------|
| ~~F14~~ | ~~Confirmar diagnóstico de tráfego~~ | ✅ **Respondido 25/07** — GA4 (28d) mostra 230 usuários reais/mês (Bing orgânico 179 sessões, Direct 52, Yahoo referral 28, **AI Assistant/ChatGPT+Copilot 17**, Google orgânico: **0**). GSC (3 meses) confirma: 1.133 impressões BR, só 2 cliques. Não é ausência de tráfego, é ausência de tráfego **do Google** especificamente — ver `MEMORY.md` |
| F15 | Backlinks — plano fechado (25/07, ver `MEMORY.md`) | **1º link executado (05/08):** Paulo comprou/arranjou placement em `band.com.br` (Grupo Bandeirantes, TV nacional), matéria editorial de 27/07 sobre isenção de IR linkando dofollow para `salario-liquido` (âncora "cálculo salário líquido") — domínio de autoridade bem acima do que o plano original cogitava (diretórios/guest posts). **2º link executado (12/08):** placement em `acritica.com` (A Crítica, jornal regional do Amazonas), matéria de 12/08 sobre descontos no 13º linkando para `decimo-terceiro` (âncora "calcular décimo terceiro", `rel="noreferrer"` — sem `nofollow`, único link editorial em texto da página) — dentro do prazo de setembro, mesmo patamar de autoridade do 1º link. **Links 3 e 4 decididos em 14/09 (ver `MEMORY.md` → "Plano de backlink", parte 7); Paulo compra conforme conseguir os placements, sem prazo fixo.** O dado que guiou a escolha: **os 2 primeiros não moveram posição** (`salario-liquido` 54,8 → 69,4; `decimo-terceiro` 76,6 → 86,5, com impressões de 87 → 277) — o que não prova que link não funciona nessa SERP, prova que **um** link por página não resolve. **3º: `rescisao-trabalhista`**, maior cluster sem link (Semrush 34.290 buscas/mês, 54 kws; 330 impressões no GSC) e centro do grafo interno — hub do F58, 4 posts do F60 e guia do F22 já apontam para lá, e com AS 2 o PageRank interno é o único capital de autoridade que existe; âncora **"calcular rescisão do contrato de trabalho"** (590/mês, KD 17). **4º: `decimo-terceiro` de novo, de propósito** — segundo link na mesma página para separar "link não funciona" de "um link não basta", que é a dúvida que trava o orçamento desta feature desde agosto; âncora **"cálculo de 13 proporcional"** (320/mês, KD 16), obrigatoriamente diferente da do `acritica.com`. Ressalva registrada: de 86,5 não se chega à página 1 até novembro — não é aposta no pico deste ano. **Descartadas com número:** `financiamento` (KD 49-65 no cluster inteiro, termo de banco/fintech), `irrf`/`inss` (o `inss` não tem **uma keyword no top 100** e o `irrf` tem 3 — e os dois já ganham no canal que não depende de posição). `hora-extra` fica como alternativa ao 4º **se a escolha for espalhar em vez de testar profundidade**. **Medir o efeito no Bing Webmaster Tools, não no GSC** — o site rankeia em 4-9 no Bing e o BWT não tem a média de 90 dias. Demais candidatas originais (`poupanca`, `ferias`, `fgts`) seguem na fila. **4º publicado em 15/09** (`mercadohoje.uai.com.br`, dofollow, editorial). **3º publicado em 21/09, em veículo trocado:** `jornaldebrasilia.com.br` em vez do `msnoticias`, com destino e âncora do plano, dofollow e único link externo do corpo, mas **na seção `/brasil-7/`, que é balde de link pago** (ressalva no Google). **O `msnoticias` não vem (Paulo, 22/09): o Jornal de Brasília o substituiu, e o plano de 14/09 fecha em 4 links.** **Base do BWT para medir, pela posição (08→20/09):** `rescisao-trabalhista` 401 impressões / 11 cliques @ **7,29** (link 3 publicou depois da janela) e `decimo-terceiro` 51 / 0 @ **8,39** (a impressão do 13º sobe sozinha com a sazonalidade, então ela não serve de medida) |
| ~~F16~~ | ~~`llms.txt` + GEO (SEO de IA)~~ | ✅ **Entregue 27/07** — ver Parte 1 |
| F17 | Google Ads — piloto de aquisição paga, mesmos alvos do F15 | Produto já está validado (233 `calculator_calculated` em 28 dias via outros canais) — o piloto não precisa provar que o produto funciona, precisa gerar tráfego/dado **especificamente atribuível ao Google** enquanto backlinks maturam. Orçamento/duração/CPC (Keyword Planner ou Semrush) pendentes de decisão do Paulo |
| ~~F18~~ | ~~Investigar Rich Results (FAQPage/HowTo sem aparecer no GSC)~~ | ✅ **Respondido 27/07 — não é bug, é o Google inteiro, e só afeta o dropdown visual, não o conteúdo.** `HowTo` rich result foi **descontinuado (desktop e mobile) desde set/2023** — nunca teria chance de aparecer, mesmo com o F12 implementado corretamente. `FAQPage` rich result já estava restrito a domínios "autoritativos" de governo/saúde desde ago/2023 e foi **retirado por completo de todos os sites em 07/05/2026** (~2,5 meses antes desta checagem, dentro da janela de 3 meses analisada no GSC). **Importante: só o dropdown expansível na SERP morreu — o texto das perguntas/respostas (F10, 101 perguntas/20 calculadoras) continua valendo como conteúdo normal de SEO** (casa a página com buscas no formato de pergunta, cobertura semântica do cluster, fonte de citação pra IA/GEO). O JSON-LD `FAQPage`/`HowTo` em si é inofensivo de manter (Google confirma que não precisa remover), só não gera mais exibição visual — ver `MEMORY.md` |
| F19 | Sprint 1.6 — AdSense | Gate `Fase 1 → Fase 2` do `docs/PLANO_IMPLEMENTACAO.md`. **Metade entregue em 08/09 (v0.30.1): conta criada pelo Paulo, site verificado (meta + `ads.txt`), exibição desligada.** O que resta: ~~(1) concluir a verificação no painel **depois do deploy**~~ ✅ **feito — em 10/09 o painel mostra `calculosonline.com.br` como "Preparando" com `ads.txt` "Autorizado"**, ou seja, posse verificada e `ads.txt` lido e aceito; o site está em revisão e não há ação pendente nessa frente; (2) decidir **onde** o `AdSlot` entra (sugestão: só abaixo do resultado, nunca acima do formulário, conferindo o bounce do `cdb` (0,86) e da `poupanca` (0,71) antes/depois); (3) **reescrever o gate**, pendente desde 20/08 — "esperar tráfego do Google" entregou 4 sessões em 8 meses e no export de 08/09 entregou **zero**, enquanto há ~180 usuários/mês de outros canais. Trocar por "N usuários/mês de qualquer canal", senão a espera é indefinida |
| F68 | **Calculadora de datas — a maior aposta de expansão, e a primeira sem legislação para manter.** Saiu da comparação com dois concorrentes (Diário 22/09, partes 9 e 10): o `calculadora.com.br` tem **247 domínios referentes e 653 backlinks — a nossa ordem de grandeza — e 21.700 visitas/mês contra a nossa 1**, e **duas páginas de datas fazem 66% disso**; o `calculaonline.com.br` tira 762 visitas da dele. Uma única página (`/data/operacao-datas`) rankeia para **49 keywords**, quase todas em posição 3-5. **Por que esta categoria e não mais calculadora trabalhista:** (1) existe KD 16-25 de verdade na cauda — `diferença entre datas` 4,4K/**KD 16**, `diferença entre duas datas` 170/**12**, `dias entre datas` 3,6K/21, `somar dias` 3,6K/25 —, que é a faixa que AS 2 alcança, contra KD 30-40 do trabalhista onde **três** sites independentes empacam em 26-58; (2) **data é aritmética: zero tabela anual, zero risco de repetir o F64**; (3) uma página cobre dezenas de sinônimos se a copy carregar o vocabulário (disciplina do F38/F39/F53/F67); (4) serve os dois índices — o painel de SoA do Clarity já trouxe pergunta de data em linguagem natural ("se o dia 20 cai no domingo em que dia eu tenho que receber meu vale"). **Escopo proposto (ampliado em 22/09 pela análise do `calculadoraonline.com.br`, parte 12):** um motor de aritmética de tempo servindo três frentes — **(a) datas** (somar/subtrair dias, contar dias entre datas, meses e anos) + dias úteis; **(b) horas** — `calculadora de horas` tem **165 mil buscas/mês com KD 19 e o líder do nicho está em 25º**, a melhor relação volume/dificuldade de todas as análises do dia, com `contador de horas` (22,2K, KD 31) e `contar horas` (6,6K, KD 30) ao lado; e já temos motor de hora no F48 (jornada, minutos, conversão de 52min30s); **(c) contagem regressiva** — `quantos dias faltam para o natal` 90,5K (KD 31), `contagem regressiva` 60,5K (37), `quantos dias faltam para acabar o ano` 33,1K (33), `quantos dias faltam para o meu aniversário` 1,6K (**KD 18**): páginas curtas que voltam todo ano, com pouca gente disputando. Tudo com a cauda de sinônimos na copy. **Alvo de entrada definido em 22/09 pelo export do `supercalendario.com.br` (parte 14), que é quem realmente domina este mercado** — uma única página dele, `/calculadora-data`, faz **20.416 visitas/mês com 35 keywords**, mais que o site inteiro do `calculaonline`: **não mirar `calculadora de datas`** (33,1K mas KD 37, com um AS 20 em 1º) e sim a cauda de **KD 16-25**, onde as medições concordam — `diferença entre datas` 4,4K/**16**, `dias entre datas` 3,6K/21, `calculadora de meses` 5,4K/23, `calcular dias` 6,6K/**24 com o líder do nicho em 9º**, `somar data` 1,3K/24, `somar dias` 3,6K/25, `contador de meses` 5,4K/25. Essa cauda soma ~30K de volume/mês na faixa que AS 2 alcança. **Ressalva de posicionamento:** quem ganha essas buscas são sites de **calendário**, não de calculadora — o contexto mental da busca é o calendário, e é por isso que o `calculadora.com.br` fica em 4º e o `calculaonline` em 10º-19º. **E é a vizinhança mais citada pela IA do Google de todas as analisadas** (o `supercalendario` tem AI Visibility 22 e 18 menções em AI Overview), onde hoje temos zero — a F68 é a única aposta que ataca os dois índices ao mesmo tempo. **Ressalva registrada:** 20-30% do tráfego dos dois concorrentes vem de domínio de casamento exato (`calculadora`, 6,1M buscas; `calcula online`), e isso **não é replicável** — o que é replicável é a escolha de tema e o formato de página única |
| ~~F69~~ | ✅ **Entregue 2026-09-22** (v0.36.0) — ver Parte 1. ~~Página "férias + 13º" — a intenção combinada que calculadora de item único não atende.** O `ferias-13` do `calculaonline` ocupa **posição 1-3** numa família de ~18 keywords de **KD 11-24** e rende ~350 visitas/mês: `calcular decimo terceiro e ferias` (390/mês, KD 15, **pos. 1**), `calculo de 13 e ferias` (110, KD 13, pos. 1), `calculadora de 13 e ferias` (90, **KD 11**, pos. 1). É o **menor KD que já apareceu em qualquer análise deste projeto**, e a pergunta é real: quem vai sair de férias em novembro/dezembro recebe as duas coisas na mesma janela e quer o total. Já temos as duas calculadoras, o motor de férias líquido (F66) e o hub do F58 — que **não** atende esta intenção, porque ele mira rescisão e soma quatro blocos. **Sazonalidade: a janela é out-dez**, a mesma do 13º |
| F70 | **Cliques mortos no hub e no resultado.** O Clarity mede: no hub do F58, **45% dos cliques nos cards de resultado erram o alvo** (caem no `LI` em vez do `SUMMARY`), e "Clique inativo" aparece em 16-18% das sessões; na rescisão, **10 cliques no valor grande do resultado e ~15 nas linhas do detalhamento**, que não são clicáveis. **Correção proposta:** o gatilho do `<details>` passa a ser o cabeçalho inteiro do card, e o valor do resultado ganha copiar-ao-clicar (que é provavelmente o que a pessoa está tentando fazer). É a feature mais barata da fila e melhora a experiência do tráfego que **já existe** — o hub empata com a home em sessões no Clarity |
| F71 | **Calculadora de folha de pagamento — a mesma conta que já temos, vista do lado do empregador.** Achada no `calculadoraonline.com.br` (Diário 22/09, parte 12): 13 keywords em **posição 1-3** com KD **21-33** — `folha de pagamento como calcular` 2,9K (KD 25), `calculadora folha de pagamento` 1,9K (28), `cálculo da folha de pagamento` 1,3K (**21**), `calculo de folha de pagamento` 2,9K (33). **O KD é mais baixo que o de `calculo salario liquido` (35), onde três sites independentes empacam em 42-58.** Volume é modesto, mas a intenção é de empregador/RH — público de CPC melhor para quando o AdSense entrar (F19). **Já temos as peças:** INSS, IRRF, FGTS e salário líquido no motor; falta a embalagem — o custo total do empregado (salário + encargos + provisões de 13º e férias), que é o que a busca quer e o que nenhuma das nossas 20 páginas responde |
| F72 | **Calendário de feriados — o cluster mais barato de produzir que apareceu, e a F68 já precisa do dado.** Achado no `supercalendario.com.br` (Diário 22/09, parte 14): `feriados 2027` 22,2K de volume com **KD 16** (ele em 5º), `feriados` 74K/KD 25 (em 10º), `hoje é feriado` 110K/**KD 29** (em **22º**) — volumes grandes com KD que AS 2 alcança, e **ninguém forte no topo** da cauda. **É dado estruturado:** a lista de feriados nacionais é pública, muda uma vez por ano e **já é insumo obrigatório do cálculo de dias úteis da F68** — o custo marginal é a página, não o dado. Formato provável: uma página por ano (`/feriados/2027`) mais a resposta direta de "hoje é feriado?". **Depende da F68 entrar primeiro**, por dois motivos: reaproveita o motor de datas e, principalmente, a F68 é que vai provar se o canal responde — sem isso, isto aqui é aposta sobre aposta |

### ~~Bloco A — aquisição, barato e alta confiança (export de 27/08)~~

✅ **Entregue 2026-08-27** (F43-F46) — ver Parte 1. Os dois itens que dependiam
de painel estão fechados: o F46 exigia o secret `INTERNAL_API_KEY` no GitHub
(**criado em 30/08**) e o F45, o evento principal no GA4 (**marcado em
10/09**); ambos os procedimentos estão na seção "Operação" do
[`README.md`](README.md).

### ~~Bloco B — fechar as lacunas que a SERP dos concorrentes expõe (export de 27/08)~~

✅ **Entregue 2026-08-27** (F47-F51, `v0.23.0`) — ver Parte 1.

### ~~Bloco C — GEO/IA, o único canal com crescimento provado (export de 27/08)~~

✅ **Entregue 2026-08-27** (F52-F55, `v0.26.0`) — ver Parte 1.

### Bloco D — dependem de decisão do Paulo (export de 27/08)

Registrado em 2026-08-27 (ver [`MEMORY.md`](MEMORY.md), diário 2026-08-27, §7).
Três dos quatro itens já têm número atribuído em outras seções — ficam aqui só
como referência cruzada, com o contexto novo do export.

| # | Feature | Contexto |
|---|---------|----------|
| ~~F56~~ | ~~**Investigar o mobile**~~ | ✅ **Entregue 2026-08-29** — ver Parte 1 (v0.27.0). **Não era performance** (Lighthouse mobile 94, LCP 2,9 s): 9 das 20 páginas rolavam na horizontal porque `mdx-components.tsx` estava fora dos globs do Tailwind e o `overflow-x-auto` das tabelas era classe morta. Campos a 16px de quebra (iOS = 27 dos 50 usuários mobile) |
| F22 | Blog sazonal — **começar pelo 13º salário** | Já registrado abaixo. **Contexto novo (27/08): a janela é agora.** A busca por 13º pica em nov/dez; publicar em setembro dá 60-90 dias de maturação. Se for para fazer algum dia, é este o mês |
| F15 | Backlinks — links 3 e 4 escolhidos (14/09) | Alvos e âncoras no P0 acima; detalhe em `MEMORY.md` → "Plano de backlink", parte 7. **Paulo compra conforme conseguir os placements — sem prazo, e nada mais do roadmap se move até lá.** Ao fechar cada um: conferir **dofollow no HTML bruto** (não confiar no vendedor), âncora embutida em frase natural, e veículo editorial de finanças/RH/notícia regional (`band.com.br` e `acritica.com` são o patamar). **Os dois foram encomendados pelo Paulo em 14/09**, nas páginas e com as âncoras recomendadas: **3º — `msnoticias.com.br`** (MS Notícias, jornal regional do Mato Grosso do Sul, mesmo perfil do `acritica.com`) → `rescisao-trabalhista`, âncora `calcular rescisão do contrato de trabalho`; **4º — `mercadohoje.uai.com.br`** (editoria de finanças do portal UAI, grupo **Diários Associados**, mesmo do Estado de Minas) → `decimo-terceiro`, âncora `cálculo 13 proporcional` — tematicamente o melhor placement dos quatro, uma vertical de finanças é mais relevante que jornal generalista. **Risco identificado antes da publicação, específico do 4º: o `mercadohoje` exibe blocos "Publicidade" e não declara política de rotulagem de patrocinado — se a matéria cair em slot publieditorial o link pode sair com `rel="sponsored"`/`nofollow` e não passar autoridade nenhuma. Conferir o `rel` no HTML bruto é a primeira coisa a fazer quando publicar.** Checklist ao publicar cada um: (1) `rel` sem `nofollow`/`sponsored`/`ugc` no HTML bruto; (2) âncora exata em frase natural; (3) página sem `noindex`/`X-Robots-Tag`, self-canonical, seção liberada no `robots.txt`; (4) conferir no **BWT → Vínculos regressivos** (não no GSC) — o esperado é ir de **2 para 4** domínios, com o `mercadohoje` entrando como domínio próprio por ser subdomínio; (5) acompanhar posição no **BWT**, não no GSC. ✅ **Pendência de 20/08 encerrada, com correção do critério:** o `acritica.com` **continua não aparecendo** no GSC → Links 33 dias depois (o `band.com.br` levou ~1-2 semanas), o que pelo critério registrado significaria "o link não conta". **Verificação da matéria derruba o critério, não o link:** página em HTTP 200, link ainda presente com a âncora exata `calcular décimo terceiro`, **sem `nofollow`/`sponsored`/`ugc` em lugar nenhum**, zero ocorrências de `noindex`, sem `X-Robots-Tag`, canonical self-referencing e `/economia/` liberado no `robots.txt` — e o `rel="noreferrer"` aparece 12× na página, ou seja é padrão do template para todo link externo, não algo aplicado ao link pago. **Regra que fica: `GSC → Links` amostra e atrasa, e não serve como prova de ausência — só o HTML bruto decide se um link conta.** **E o BWT encerrou o caso no mesmo dia:** "Vínculos regressivos" lista **os dois** domínios — `acritica.com` (1) e `band.com.br` (1). O link conta; quem falhou foi o relatório do Google. **Achado maior: o BWT é o placar correto de backlink deste projeto** — Semrush diz 122 domínios referentes, GSC diz 1, **BWT diz 2, exatamente os dois placements editoriais reais**, confirmando por terceira fonte independente a hipótese de 09/08 de que os 122 do Semrush são scraper/agregador. **O site tem 2 backlinks reais e os 2 encomendados dobram isso (2 → 4)** — é assim que a expectativa deve ser dimensionada. **Risco a monitorar:** os mesmos 2 links convivem com posição **4-9 no Bing** e **85-95 no Google** — o que difere não é o link, é o limiar de cada buscador. Se 3 e 4 também não moverem o Google, a decisão vira de ordem de grandeza (subir muito o orçamento, ou tirar o Google do plano e organizar o projeto em torno de Bing + IA). **Publicados (ver `MEMORY.md`, diários de 15/09 e 22/09):** 4º em 15/09 no `mercadohoje`, limpo. 3º em 21/09 no **`jornaldebrasilia.com.br`**, não no `msnoticias` ([matéria](https://jornaldebrasilia.com.br/brasil-7/demissao-por-acordo-o-que-o-trabalhador-do-df-recebe-e-o-que-perde/)). Passa os itens 1-3 do checklist e tem o melhor encaixe temático dos quatro (demissão por acordo, simulando as três modalidades que o `RescisaoForm` calcula), mas **saiu na `/brasil-7/`**: 972 posts no estilo "10 melhores…", cada um com um link comercial, fora da editoria `/noticias/`. Pode ser neutralizado no Google, e é por isso que ele vale menos que os outros como prova de "link move o Google?". No Bing deve contar. Os sitemaps do jornal estão congelados desde 18/03/2026, então o link depende da paginação da categoria para ser descoberto. **Item 6 acrescentado ao checklist:** abrir a seção e 2-3 vizinhos, e pedir ao fornecedor a URL da seção antes de pagar |
| F19 | Rever o gate do AdSense | Já registrado no P0. Pendente desde 20/08: trocar "esperar tráfego do Google" (que entregou 4 sessões em 8 meses) por "N usuários/mês de qualquer canal", senão a espera é indefinida |

~~**Ainda sem número, só no `MEMORY.md` (§P1):** hub "Calculadora Trabalhista
Completa"~~ — ✅ **Entregue 2026-09-01 como F58** (v0.29.0), ver Parte 1.

### Prioridade imediata (produto)

| # | Feature | Contexto |
|---|---------|----------|
| F20 | Sprint 1.5 — PWA + Android (TWA/bubblewrap) | Próxima sprint do plano de implementação (`AGENTS.md`). Paulo deve confirmar se ainda fura a fila à frente de F15-F18 |
| ~~F21~~ | ~~Centralizar SEO como no Recibo Fácil~~ | ✅ **Entregue 2026-08-05** — ver Parte 1 (v0.7.0) |
| ~~F22~~ | ~~Blog sazonal~~ | ✅ **Entregue 2026-08-30** — ver Parte 1 (v0.28.0). A cauda longa que o GSC mostra (“calculadora férias 2025/2026”, “como calcular hora extra”) agora tem onde ser atendida sem página de calculadora nova |
| ~~F35~~ | ~~Replicar o padrão de UX do F34 nas outras 19 calculadoras~~ | ✅ **Entregue 2026-08-06** — ver Parte 1 (v0.9.0) |

### Growth / SEO (impacto médio, mais barato)

| # | Feature | Contexto |
|---|---------|----------|
| F23 | Cauda longa via páginas programáticas | `Consultas.csv` do GSC mostra 450+ variações de query para os mesmos ~15 conceitos (ex. 37 variações só de "calculadora férias"), quase todas com posição pior que 60 — cuidar de canonical alternates para não gerar duplicate content. **Reduzido em 20/08:** F38 e F39 já cobriram os dois maiores clusters de vocabulário (simulador, demissão) sem página nova nenhuma — vale medir o efeito deles antes de investir em geração programática |
| F24 | Confirmar hreflang/geo-targeting | Tráfego internacional (Índia, Filipinas, Vietnã, 0 cliques) confirmado de novo no export 25/07 — baixa prioridade, só validar que não é bug |

### Estratégico — Fase 2 (pausada)

| # | Feature | Contexto |
|---|---------|----------|
| F26 | Fase 2 do plano de negócios (+30 calculadoras) | **Pausada** (decisão de 2026-07-19, reafirmada em 25/07 com dado real de tráfego) até o Google mandar tráfego para as 20 atuais — ver `MEMORY.md`. Candidatos já registrados: categoria "Tempo" (7 calculadoras) + expansões de Saúde/Negócios/Financeiras — ver `MEMORY.md` §Candidatos de expansão de catálogo. **Nenhum implementado; validação de volume/KD no Semrush ainda não foi feita.** |
