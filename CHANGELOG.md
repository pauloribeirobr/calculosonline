# Changelog — Calculos Online

Funcionalidades entregues em produção. Versionamento SemVer (`Major.Minor.Patch`)
do app `web` (`apps/web/package.json`): `minor` para nova funcionalidade, `patch`
para ajuste/correção, `major` para marco de estabilização. Histórico anterior à
estabilização fica em `0.x`.

- **Stack atual:** Next.js `15.5.18` (React `19.0.0`), monorepo Turborepo com
  `packages/core` (cálculos) e `packages/ui` (componentes) versionados à parte.
- **Coluna Versão:** versão de `apps/web` *após* aquela entrada.
- Entradas mais recentes no topo. Datas em `AAAA-MM-DD`. `Commit` = commit principal.

| Data | Versão | Funcionalidade | Commit |
|------|--------|-----------------|--------|
| 2026-08-27 | — | **F46 — IndexNow automático depois do deploy de produção.** O script existia desde 20/08 mas **nada o disparava**: sem hook, cron ou build step, e ele precisa rodar *depois* do deploy (antes, os buscadores rastreiam o conteúdo antigo). Com o Bing em ~95% do tráfego real — 462 sessões contra 4 do Google no GA4 de vida inteira — era o passo de maior retorno e o mais fácil de esquecer. `.github/workflows/indexnow.yml` escuta `deployment_status` e submete o sitemap inteiro. **Dois filtros que não são óbvios:** o evento dispara em *todo* estado (`pending`/`failure`/`success`) e em *todo* ambiente, então o `if` exige `state == 'success'` **e** `environment == 'Production'` — sem o segundo, todo deploy de preview submeteria URLs de produção. Sem `pnpm install` nem build no job: o script não tem dependência (só `fetch` nativo) e lê a lista de URLs do `sitemap.xml` em produção. Também há `workflow_dispatch` com campo de slugs para submissão manual pelo GitHub. **Exige setup de uma vez:** secret `INTERNAL_API_KEY` no repositório, mesmo valor da Vercel | — |
| 2026-08-27 | — | **F45 — evento de conversão do GA4 documentado.** Não há código a mudar: `calculator_calculated` já é disparado corretamente por `analytics.calculatorCalculated()` (385 eventos em 28 dias no export anterior). O que falta é marcá-lo como *key event* no painel do GA4 — a API de coleta não faz isso. Sem a marcação, o GA4 reporta "Leads qualificados 0" em todas as semanas desde janeiro e o Google Ads não tem sinal para otimizar, que é a razão real de o F17 estar travado. Procedimento na seção "Operação" do `README.md`, com as duas ressalvas que costumam morder: o GA4 **não retroage** (a contagem começa quando a chave é ligada) e o evento só aparece na lista se foi coletado nas últimas 48h — ausência ali é falta de cálculo no período, não tracking quebrado | — |
| 2026-08-27 | **0.18.0** | **F44 — URLs quebradas com tráfego real.** O Clarity (25-27/08, 25 sessões) mostrou `/site` respondendo por **4 sessões, 16% do total**, e `/2026/calculadora/cdb` por 1; nenhuma existe em `app/` e não havia bloco `redirects` no `next.config.ts`. Como a origem é externa (link errado em diretório/agregador), não dá para corrigir na fonte — o redirect recupera a sessão em vez de servir 404. **O achado maior veio do código, não dos dados:** o `Footer.tsx` linkava `/blog`, rota que **nunca existiu**, em **todas as páginas do site** — 404 interno rastreado pelo Google em 100% dos crawls, queimando exatamente o PageRank que o F43 redistribui. Entregue: bloco `redirects` (`/site`, `/site/:path*` e o prefixo de ano espúrio `/:ano(\d{4})/calculadora/:slug` → `/calculadora/:slug`, todos 308) e link morto removido do rodapé. Travado em `tests/e2e/link-interno.spec.ts` | — |
| 2026-08-27 | **0.17.0** | **F43 — escultura de link interno.** O export do GSC de 27/08 mostrou **31 links internos idênticos** para todas as páginas — o `Footer.tsx` listava as 20 calculadoras em todo lugar, e quando tudo linka tudo nenhum link significa nada — e o único diferencial real, `relacionadas` do registry, estava **invertido**: `financiamento`, maior página do site (298 impressões), recebia **1** link; `juros-compostos` (3 impressões, pos. 97,7) recebia **8**. Com Authority Score 2 e um único backlink reconhecido, o PageRank interno é o único capital de autoridade 100% sob controle. Três frentes: **(1)** as 20 listas de `relacionadas` reescritas por impressão do GSC — `financiamento` 1→6, `fgts` 2→6, `hora-extra` 2→5, `ferias` 4→5, contra `juros-compostos` 8→4, `salario-liquido` 7→3 e `inss` 5→3; a inversão a corrigir era de direção, não de volume, e as duas páginas com backlink externo real (`salario-liquido`/band.com.br e `decimo-terceiro`/acritica.com) passaram a apontar **para** os alvos do P0 — são fonte de autoridade, não destino. **(2)** Rodapé de 20 links para 8 destaques (ordem = impressões) + as 6 categorias + "Todas as calculadoras", com `/categorias` promovido a hub; o grid aninhado 2+3 virou um `md:grid-cols-3` único. **(3)** **20 links contextuais dentro dos MDX, onde antes havia exatamente zero** nos 20 arquivos — todo link interno vinha do rodapé ou do bloco `relacionadas`, ambos fora do corpo do texto, que é justamente onde o anchor text carrega contexto semântico. Efeito colateral previsível no teste: o rodapé passou a repetir os nomes de categoria e quebrou o modo estrito do locator em `categorias-navegacao.spec.ts`, corrigido escopando as asserções em `main` (que é a asserção certa de qualquer forma). `seoRefreshDate` bumpada para 2026-08-27 — sem isso o sitemap declararia que nada mudou e todo o resto seria inócuo | — |
| 2026-08-20 | **0.16.0** | **F42 — og-image gerada por calculadora, a partir do ícone e da cor da categoria.** Fecha um bug que estava em produção desde o início: `seo.ts` apontava `openGraph.images` e `twitter.images` de **toda** página para `/images/og-image.png`, arquivo que **nunca existiu** — todo link compartilhado no WhatsApp pelo F32, todo card social e toda citação de IA mostravam preview quebrado. Agora cada calculadora tem a sua imagem (`app/calculadora/[slug]/opengraph-image.tsx`, 1200×630 via `next/og`) com o próprio ícone, a cor da categoria em faixa e pastilha, `tituloLongo`, `descricaoCurta` e o selo "Grátis, sem cadastro" — mesma identidade do F41, agora na superfície mais compartilhada que o site tem. Mais uma imagem de marca do site em `app/opengraph-image.tsx`, com as 6 categorias em pastilhas. São 20+1 imagens geradas **no build** (`generateStaticParams`), sem custo de runtime; o build foi de 40 para 61 páginas. **Três obstáculos reais, todos documentados no código:** (1) o Satori **não renderiza componentes `forwardRef`**, que é o que os Heroicons são — `<Icone />` produzia o quadro vazio, sem erro nenhum; a saída foi desembrulhar o `.render()` e entregar a árvore `svg > path` (novo `glifoHeroicon` em `lib/iconesCalculadora.ts`, módulo extraído do `CalculatorIcon.tsx`); serializar com `react-dom/server` seria o caminho óbvio, mas o Next proíbe esse import dentro de `app/`. (2) `div` com mais de um nó filho exige `display` explícito no Satori — `{n} texto` são dois nós. (3) **As duas armadilhas opostas do App Router**: definir `openGraph.images` **sobrescreve** a convenção `opengraph-image.tsx` (foi o que manteve o 404 por meses), e declarar `openGraph` **sem** `images` *suprime* a herança do segmento pai — por isso `/sobre`, `/categorias` e `/contato` ficaram sem imagem nenhuma enquanto a home, que não exporta `metadata`, tinha. `buildMetadata` ganhou `imagemPropriaDaRota` para distinguir os dois casos. Nenhuma paleta em hexadecimal foi duplicada: `IdentidadeCategoria` ganhou o campo `familia` e os hexes saem de `tailwindcss/colors`. Corpo do título adaptativo por comprimento (de "Calculadora de IMC" a "Calculadora e Simulador de Tesouro Direto"), senão o rodapé era empurrado para fora da imagem. 4 testes E2E novos, incluindo leitura do cabeçalho PNG para conferir 1200×630 e uma varredura que exige `og:image` respondendo 200 em toda página. | — |
| 2026-08-20 | **0.15.0** | **F41 — identidade visual unificada por categoria e ícone em todas as superfícies.** Pedido do Paulo: "associar visualmente um ícone a cada cálculo, o mesmo para identidade visual agrupada; foco em visual mais suave e fácil de mexer". O `CalculatorIcon` do F7 já tinha 20 ícones e 6 paletas — o problema era **onde ele não chegava** e **quantas cópias da paleta existiam**. (1) **Fonte única**: novo `apps/web/src/lib/identidadeVisual.ts` concentra tipo, label, descrição e tokens de cor das 6 categorias. Antes eram **quatro** tabelas paralelas — `CATEGORIAS` e `CATEGORIAS_LABEL` (`calculators.ts`, com labels idênticos entre si em todas as 6 categorias, pura duplicação) e as paletas `categoryStyles` (`CalculatorIcon.tsx`) e `categoriaCor` (`CalculatorTypes.tsx`), que descreviam as mesmas cores em formatos incompatíveis (`ring` vs `border`) e **já haviam divergido**. As duas primeiras agora derivam da fonte única; as duas paletas foram deletadas. Mudar a cor de uma categoria virou uma linha num arquivo. **O arquivo não importa React nem Heroicons de propósito** — é lido por `sitemap.ts` e `schema.ts` (server); o mapa de ícones (componentes) segue em `CalculatorIcon.tsx` e lê as cores de lá. (2) **Tokens atômicos** (`fundo`/`anel`/`borda`/`icone`/`texto`/`hover`) em vez de um blob de classes, porque os consumidores precisam de recortes diferentes. **`icone` e `texto` são separados por acessibilidade, não por estética**: ícone é objeto gráfico decorativo (piso 3:1) e fica em `-600`, texto exige 4,5:1 e fica em `-700` — medido, e o `-600` reprova para texto em âmbar, esmeralda e ciano (âmbar passa raspando no ícone, 3,07:1). (3) **Suavização**: removido o ponto decorativo `opacity-25` sobreposto ao ícone (poluía nos tamanhos `sm`/`md`), glifo de `-700` para `-600`, `ring` padronizado no lugar de `border` e raio `rounded-md`→`rounded-lg` no tamanho `sm`. (4) **Ícone onde faltava**: `CalculatorLayout` (`packages/ui`) ganhou slot `icone?: ReactNode` e as **20 páginas de calculadora** — que recebem todo o tráfego orgânico e eram as únicas sem identidade visual — passam a abrir com o ícone ao lado do H1; `RelatedCalculators` ganhou `icone` por item (reforça o link interno do N10); `/meus-calculos` resolve o ícone pelo `slug` no registry, sem migrar os registros que o usuário já tem no IndexedDB do F37. Ícone entra como `ReactNode` porque `packages/ui` não pode importar de `apps/web` — mesmo padrão de `form`/`result`/`content`. 4 testes E2E novos, incluindo a invariante de que a mesma categoria pinta igual na home e na página da calculadora. | — |
| 2026-08-20 | **0.14.0** | **F40 — as duas leituras do aviso prévio no acordo mútuo, lado a lado na rescisão.** O art. 484-A, I da CLT manda pagar "por metade o aviso prévio, se indenizado" e **não diz metade de quê** — daí duas contas que convivem na prática: metade do aviso mínimo de 30 dias (**15 dias fixos**, o que o motor fazia desde o F3) ou metade do aviso proporcional da Lei 12.506/2011 (**cresce 3 dias por ano**). As duas coincidem até 1 ano completo de casa e divergem a partir daí: com salário de R$ 3.000, a diferença no líquido é de **R$ 450 aos 3 anos e R$ 1.500 aos 10 anos**. Em vez de escolher uma leitura, a calculadora mostra as duas. **Refatoração no motor** (`packages/core/src/trabalhista/rescisao.ts`): o corpo do cálculo virou `montarRescisao(diasAvisoPrevio)`, porque trocar os dias de aviso não muda só a linha do aviso — o aviso indenizado projeta tempo de serviço, que muda férias proporcionais, 13º e, por consequência, INSS e IRRF. No acordo mútuo a função roda duas vezes e a segunda leitura sai completa, não estimada. **O resultado principal não mudou** — continua resolvendo pela leitura restritiva, que é a praticada pela maioria das empresas e portanto o valor que o usuário provavelmente vai receber; a segunda entra como duas linhas `neutro` no detalhamento (cinza, sem sinal +/−, fora da soma) mostrando o aviso alternativo e o total líquido correspondente com a diferença em reais. Novo tipo exportado `LeituraAvisoAcordo` e campo `avisoPrevioLeituraProporcional` (`null` fora do acordo mútuo e quando as leituras coincidem). `diasAvisoPrevio` passa a aceitar fracionário (19,5 dias aos 3 anos), com helper `formatarDias` para vírgula decimal. Conteúdo: seção nova "As duas leituras do aviso prévio no acordo mútuo" com tabela comparativa e 1 pergunta nova na FAQ. **5 testes unitários** novos e **4 E2E** novos (`rescisao-acordo-mutuo.spec.ts`), incluindo regressão de que a linha informativa não entra na soma. | — |
| 2026-08-20 | **0.13.0** | **F39 — vocabulário de demissão/acerto trabalhista na calculadora de rescisão (N2 do export do GSC de 20/08).** O cluster `demissão / acerto trabalhista / verbas / desligamento` somava **116 impressões em 84 queries com posição média 93,7 — a pior página do site** — e o motor já atendia todos esses casos: `MotivoRescisao` (`packages/core/src/trabalhista/rescisao.ts`) cobre `sem_justa_causa`, `justa_causa`, `pedido_demissao`, `acordo_mutuo` e `aposentadoria` desde o F3. Era gap de vocabulário, não de produto: "acerto" aparecia **0 vez** no MDX. Entregue: (1) `descricao`/`descricaoCurta` do registry reescritas em torno de "acerto trabalhista de qualquer demissão"; (2) `sinonimos` novo com as 6 formas populares (`calcular demissão`, `acerto trabalhista`, `cálculo de pedido de demissão`, `rescisão por comum acordo`, `cálculo de desligamento`, `direitos trabalhistas na saída da empresa`); (3) H2 de abertura do MDX passa a citar "e Demissão" + parágrafo explicando que acerto/conta de rescisão/verbas são a mesma coisa; (4) **seção nova "Cálculo por motivo de saída da empresa"** com um H3 por modalidade (sem justa causa, pedido de demissão, comum acordo, rescisão indireta, justa causa), cada uma nomeando as verbas devidas; (5) **5 perguntas novas** na FAQ (`Como calcular o acerto trabalhista?`, `Quanto vou receber se pedir demissão?`, `Como calcular a rescisão por comum acordo?`, `A calculadora serve para rescisão indireta?`, `Qual a diferença entre acerto trabalhista e verbas rescisórias?`), que entram automaticamente no `FAQPage` do JSON-LD — a página foi de 6 para 11 perguntas. **Correção factual de quebra:** o MDX afirmava em dois pontos que no acordo mútuo "o FGTS pode ser sacado integralmente" — o art. 484-A, §1º da CLT limita a movimentação a **80% dos depósitos**. Corrigido. | — |
| 2026-08-20 | **0.12.0** | **F38 — vocabulário "simulador/simulação" nas 5 calculadoras financeiras (N1 do export do GSC de 20/08).** O cluster somava **180 impressões em 97 queries com posição média 83,3**, e o Semrush (19/08) mostrou que `simulação tesouro direto` tem **2.9K buscas/mês** — a maior keyword do site por volume — com a página em posição 78. A palavra não aparecia em **nenhum** title, H1, MDX ou FAQ do projeto (só em `palavrasChave` de 2 registros). Mesmo padrão do F27 (que trouxe "décimo terceiro" por extenso), com alvo bem maior. Entregue: (1) campo `sinonimos?: string[]` novo no registry, alimentando o `keywords` da página e servindo de referência editorial; (2) `tituloLongo` de `tesouro-direto`, `financiamento`, `poupanca`, `cdb` e `emprestimo` vira "Calculadora **e Simulador** de X" — muda `<title>` e `<h1>` juntos; (3) `descricao`/`descricaoCurta` reescritas com as formas nominais ("simulador de X", "faça a simulação"), que é o que o usuário digita — o texto antigo usava só o verbo ("Simule..."); (4) H2 de abertura + parágrafo novo em cada um dos 5 MDX explicando a página como simulador; (5) **1 pergunta nova por calculadora** na FAQ, cada uma capturando uma query real do GSC (`simulador CDB prefixado`, `simulador poupança Caixa`, `simulador de crédito imobiliário`, CET na simulação de empréstimo, e a comparação com o simulador oficial do Tesouro). **`buildCalculatorTitle` ganhou orçamento de caracteres** (`TITLE_MAX = 78`, já contando o template ` \| Calculos Online` do root layout): o USP "sem Cadastro" fica sempre que couber e sai quando não cabe — ele custa 14 caracteres e rendeu **4 impressões em 3 meses**, enquanto o sinônimo que ocupa o lugar vale 2.9K buscas/mês. Efeito colateral positivo: `decimo-terceiro`, que tinha o pior title do site (**90 caracteres**, truncado na SERP), caiu para 76. Novo teste E2E percorre as 20 calculadoras validando o orçamento. | — |
| 2026-08-13 | **0.11.0** | **F37 — histórico local de cálculos (IndexedDB), nas 20 calculadoras.** Ideia veio do módulo `history`/`LocalStorageRepository` do Recibo Fácil (que persiste documentos gerados) — Paulo pediu a mesma "forma de pensamento" aqui, mas com todas as calculadoras habilitadas de saída, sem seleção prévia: o usuário decide, por cálculo, se quer salvar. **Novo `apps/web/src/lib/calculationHistory.ts`**: repositório IndexedDB próprio (`calculosonline-historico`/store `calculos`), bem mais simples que o do Recibo Fácil — lá o gargalo era `Blob` de PDF estourando quota e exigindo migração de/para `localStorage`; aqui cada registro é só JSON pequeno (slug, título, categoria, `inputData`, resultado headline + formato, timestamp), sem Blob e sem fallback de storage necessário. **Reaproveita a serialização do F32** (compartilhamento por link): `inputData` salvo é o mesmo payload que já alimenta `encodeShareData`/`buildShareUrl` — reabrir um cálculo salvo é navegar pra `/calculadora/[slug]?d=<inputData codificado>`, sem lógica nova de reidratação de formulário. **`CalculatorResult`** (`packages/ui`) ganhou o botão "Salvar cálculo" (ícone `BookmarkIcon`, ao lado do "Compartilhar via WhatsApp" existente) com estado local `idle/salvando/salvo/erro` — o componente só cuida do feedback visual, quem persiste é o app via prop `onSalvarCalculo`, resetado a cada novo resultado. **Nova página `/meus-calculos`** (`apps/web/src/app/meus-calculos/`): lista os cálculos salvos (mais recente primeiro), com "Abrir" (mesma URL `?d=`, agora acrescida de `&calc=<id>`) e "Remover"; estado vazio e estado de IndexedDB indisponível tratados; `robots: noindex` (conteúdo é local ao navegador de cada visitante, não existe pra indexar) mas `follow` (não corta o link interno). **Link novo no Header** (`ClockIcon`, desktop + mobile) — único acréscimo à navegação desde a simplificação da F28, justificado por ser funcionalidade nova, não duplicata. **Reconhecimento de "já salvo" + exclusão inline (ajuste de UX pedido pelo Paulo depois do 1º teste manual):** abrir um cálculo salvo via `/meus-calculos` reconhecia o `inputData` mas oferecia "Salvar cálculo" de novo, sem indicar que aquele resultado já tinha um registro — `CalculatorResult` ganhou `salvo`/`onExcluirCalculo`: quando o resultado exibido corresponde a um registro existente, o botão vira indicador "Cálculo salvo" + botão "Excluir"; editar e recalcular volta ao estado normal (é outro cálculo). `CalculadoraPageClient` rastreia isso via o novo param `calc=<id>` na URL, consumido só no 1º resultado calculado. **Bug real pego na validação e2e:** a 1ª implementação lia esse id de uma `ref` dentro de um callback inline passado a `SharedDataReader`; como a prop mudava de referência a cada render, o `useEffect` reexecutava e "ressuscitava" o id da URL mesmo depois de já consumido — editar e recalcular um cálculo salvo continuava marcado como "salvo". Corrigido trocando a `ref` por `state` (setter do `useState`, estável por natureza, mesmo padrão que `onData`/`setSharedData` já usava sem esse problema). **Analytics:** evento `calculator_saved` (mesmo padrão de `calculator_shared`/`calculator_calculated`). **e2e:** `calculation-history.spec.ts` (6 testes: salvar muda o botão e aparece em `/meus-calculos`; abrir reidrata, recalcula sozinho e já mostra "salvo"+"Excluir"; editar e recalcular volta a oferecer "Salvar cálculo" — teste que pegou o bug da `ref`; excluir direto da página de resultado; remover pela lista; estado vazio sem nada salvo). **Polimento visual dos botões (mesma sessão, a pedido do Paulo):** as 3 opções (Compartilhar/Salvo/Excluir) nasceram todas como "pills" coloridas do mesmo peso visual, competindo por atenção. Redesenhado por hierarquia: "Compartilhar via WhatsApp" continua botão pleno (ação principal); "Cálculo salvo" virou texto simples com ícone de check verde (`CheckCircleIcon`, `@heroicons/react/20/solid`), sem borda/fundo — é status, não ação, não devia parecer clicável; "Excluir" virou botão "fantasma" (só texto vermelho, fundo só no hover) — ação secundária/destrutiva, menos peso que o compartilhamento. Validação: `pnpm typecheck` limpo em `ui`/`web`, `pnpm build` limpo (40 páginas, `/meus-calculos` estática), 345/345 testes do core (motor de cálculo intocado), 44/44 e2e verdes (suíte completa, nada quebrou). | `_pendente_` |
| 2026-08-09 | **0.10.0** | **F36 — 3º modo da margem de lucro + 3 correções de SEO on-page saídas do export do GSC de 09/08.** Paulo pediu pra avaliar a pasta `gsc/` e depois investigar a fundo por que `margem-lucro` e `porcentagem` tinham 318 impressões em posição de página 1 com 0 clique. A investigação derrubou a hipótese inicial (não era CTR: em posição 9-11 o esperado eram ~3,5 cliques no total, P(0) = 15-21%) e achou coisas melhores — ver diário 2026-08-09 no `MEMORY.md` para o diagnóstico completo. **(1) Novo modo "margem desejada → preço de venda"** (`packages/core/src/negocios/margem-lucro.ts`): `MargemLucroParams` ganhou `margemDesejadaPercent`, aplicando `Preço = Custo ÷ (1 − Margem ÷ 100)` — que **não** é o mesmo que somar a margem ao custo (custo R$100 com margem de 30% dá preço R$142,86, não R$130; o erro clássico de confundir margem com markup, que a própria página já explicava no MDX mas a calculadora não deixava fazer). Precedência explícita `preço > markup > margem desejada`, e margem ≥ 100% é rejeitada com mensagem própria (preço seria infinito). `MargemLucroForm` ganhou a 3ª opção no `select` "Calcular a partir de" (já existente) + campo `margemDesejadaPercent`, sem tocar em nada mais do formulário. Motivação de aquisição, não de completude: era a intenção de maior volume do cluster (`calcular preço de venda`, 480 buscas/mês no Semrush BR, KD 20) e a calculadora não atendia; no GSC a query correspondente (`calcular valor baseado na margem de lucro`) era a de maior impressão identificada do cluster e rankeava em **66,6**, contra **21,5** da query cujo modo já existia. 7 testes novos no core (inversão margem↔preço, margem 0, margem 50% = markup 100%, precedência entre os 3 modos, 100% e negativa rejeitadas) — 18 no arquivo, 345/345 na suíte. **(2) `descricao` da porcentagem estava quebrada** (`apps/web/src/lib/calculators.ts`): terminava em "…acréscimo, desconto, percentual de e mais" — sobra de uma lista, e era exatamente esse o texto servido na `<meta name="description">` e portanto na SERP. Reescrita cobrindo as intenções reais ("quanto é X% de um valor", "quanto X representa de Y"). A da margem de lucro também foi reescrita pra citar a intenção nova (preço a partir da margem desejada). **(3) Nova flag `atemporal` no `CalculadoraRegistro`**, consumida por `buildCalculatorTitle()` (`apps/web/src/lib/seo.ts`): calculadoras cujo resultado não muda de um ano pro outro saem sem "2026" no title. Aplicada só em `porcentagem` e `margem-lucro` — as que dependem de tabela anual (INSS, IRPF, IRRF, DAS MEI e o cluster trabalhista) continuam com o ano, que ali é sinal de atualidade legítimo. **`imc`, `calorias` e `juros-compostos` estão na mesma situação e ficaram intocados** por estarem fora do escopo pedido; basta ligar a flag quando/se decidir. **(4) H1 duplicado nas 20 calculadoras** — os 20 `.mdx` de `apps/web/content/calculadoras/` abriam com `# Calculadora de X`, que o `@next/mdx` renderizava como um segundo `<h1>` dentro do `ContentLoader`, competindo com o H1 do shell da página (confirmado no HTML de produção antes do fix: `h1 count: 2` em todas). Rebaixados para `##` — **só a primeira linha de cada arquivo**, deliberadamente: `lib/faq.ts` extrai o `FAQPage` procurando `^## Perguntas frequentes` e fatiando até o próximo `^## `, então deslocar todos os níveis quebraria as 101 perguntas do F10. Verificado no build que o `FAQPage` de margem-lucro continua com as 5 perguntas certas. **e2e:** 2 testes novos em `seo-metadata.spec.ts` — calculadora `atemporal` não pode ter `/20\d{2}/` no title, e cada calculadora tem exatamente 1 H1. `palavrasChave` de margem-lucro ganhou a query real do GSC. Validação: `pnpm typecheck` limpo nos 3 pacotes, 345/345 testes do core, `pnpm build` limpo, e HTML gerado conferido diretamente (title sem ano nas duas, description nova, `h1 count: 1`, os 3 `<option>` do modo presentes, `FAQPage` intacto). `pnpm lint` com o mesmo erro pré-existente de config (`eslint-import-resolver-typescript` ausente do `node_modules`), não relacionado — mesmo erro já registrado em 0.7.0 e 0.9.0. | `_pendente_` |
| 2026-08-06 | **0.9.0** | **F35 — replicado o padrão de UX do F34 (0.8.0) nas outras 18 calculadoras.** Todos os campos monetários dos 19 formulários restantes viraram `type: 'currency'` (máscara dígitos-como-centavos), e todo campo de contagem inteira pequena (dependentes, períodos, dias, prazo em meses, idade) virou `type: 'stepper'`. Escopo deliberadamente conservador em 2 pontos: campos com casas decimais reais (horas extras, taxas de juros/SELIC, markup) **não** viraram stepper — o widget trunca pra inteiro (`Math.trunc`), então forçaria perda de precisão; e `PorcentagemForm`/`IMCForm` ficaram intocados (valores genéricos ou decimais de peso/altura, sem prefixo R$ fixo). **`itemList` só entrou na calculadora de IRRF** (`outrasDeducoes`, antes um único campo agregado — mesmo padrão do "outras deduções" do salário líquido): o core (`calcularIRRF`) continua recebendo `number`, a soma dos itens acontece no form via novo helper `apps/web/src/lib/itemListField.ts` (`itemListSchema()`/`limparItensVazios()`/`somarItens()`, reaproveitando o `somarItens(ItemValor[])` do `@calculosonline/core`) — sem mudar a assinatura pública do motor de cálculo. Não virou `itemList` nos outros 18: `IRPFForm` tem 5 categorias de dedução legalmente distintas (limites/regras próprias cada uma, não um "saco" genérico) e as demais calculadoras trabalhistas (rescisão, férias, 13º, hora extra) não têm um campo equivalente de dedução agregada no schema atual. `SalarioLiquidoForm` refatorado pra usar o mesmo helper novo (schema `itemSchema`/`listaItens` local removido, sem mudança de comportamento). **Componente compartilhado** (`packages/ui/src/CalculatorForm/index.tsx`): `StepperField` ganhou `max` opcional (`FieldMeta.max`), simétrico ao `min` já existente — usado em "Períodos de férias vencidas" (rescisão, 0–2) e "Dias vendidos/abono" (férias, máx. 10), que antes só validavam o teto na submissão. **e2e:** `calculadora-margem-lucro.spec.ts`, `share-link.spec.ts` e `quick-add-chips.spec.ts` ajustados pra semântica de máscara de centavos nos campos que mudaram de `number`/`text` pra `currency` (rescisão, CDB, margem de lucro); novo `irrf-outras-deducoes.spec.ts` (2 testes) cobrindo a soma de itens e a rejeição de linha em branco no cálculo do IRRF. Validação: `pnpm typecheck` limpo em `core`/`ui`/`web`, 338/338 testes unitários do core (motor de cálculo intocado), `pnpm build` limpo (39 páginas SSG), 41/41 e2e verdes. `next lint`/`pnpm lint` com o mesmo erro pré-existente de config (`eslint-import-resolver-typescript` ausente do `node_modules`), confirmado não relacionado a esta entrega (reproduz igual em `git stash`). | `_pendente_` |
| 2026-08-05 | **0.8.0** | **Melhorias na calculadora de Salário Líquido — campo de moeda com centavos, stepper de dependentes, deduções/descontos/adicionais itemizados.** Paulo pediu 5 melhorias de UX na calculadora (piloto — padrão a replicar nas demais depois): (1) campo de moeda com centavos, igual ao Recibo Fácil; (2) dependentes como seletor +/- em vez de input numérico cru; (3) poder detalhar cada dedução/desconto com descrição própria, não só um valor agregado; (4) "adicionais" (vale-refeição, vale-alimentação etc.) que não entram na folha mas aparecem junto ao líquido como complemento; (5) manter o compartilhamento por WhatsApp funcionando com os campos novos. **3 tipos de campo novos no `CalculatorForm` compartilhado** (`packages/ui`, reutilizáveis nas outras 19 calculadoras): `currency` (máscara dígitos-como-centavos, ex. digitar "500050" vira "R$ 5.000,50", controlado via `Controller` do react-hook-form — os chips de valor rápido continuam funcionando sem mudança), `stepper` (botões +/- com `min` configurável, usado em "Dependentes (IRRF)") e `itemList` (lista livre de itens `{descrição, valor}` via `useFieldArray`, com chips de sugestão que só pré-preenchem a descrição — decisão fechada com o Paulo: lista livre, não checklist fixo). **Motor de cálculo** (`packages/core/src/trabalhista/salario-liquido.ts`): `outrasDeducoes`/`outrosDescontos` deixam de ser `number` e viram `ItemValor[]` (novo tipo compartilhado em `types.ts`, com `somarItens()`); novo campo `adicionais`. **Bug real corrigido no caminho:** `outrasDeducoes` (plano de saúde, PGBL) só reduzia a base do IRRF — o valor não estava sendo descontado do salário líquido, apesar do rótulo indicar dinheiro que sai do contracheque; agora reduz ambos. Detalhamento passa a listar cada item pelo nome informado pelo usuário (ex. "Plano de Saúde", não mais "Outros Descontos" genérico); adicionais aparecem como linhas de crédito após "Salário Líquido", seguidas de "Total com Adicionais" (só quando há adicionais). 22 testes unitários (6 novos: itens negativos, soma de múltiplos itens, adicionais não afetam INSS/IRRF/líquido, posição da linha de total). **Testes novos:** `salario-liquido.spec.ts` (7, desktop) + `salario-liquido.mobile.spec.ts` (5, toque real via `tap()`, a pedido do Paulo — stepper por toque, área de toque ≥36px, máscara de moeda no teclado mobile) — novo projeto `mobile-chromium` (`devices['Pixel 7']`) no `playwright.config.ts`, escopado só a specs `*.mobile.spec.ts` via `testMatch`/`testIgnore` pra não duplicar a suíte desktop. **SEO (a pedido do Paulo, aproveitando o embalo):** nova pergunta na FAQ ("Como calcular o salário líquido?", cobrindo o cluster de busca "como calcular..." — ~1,7K buscas/mês) e `palavrasChave` ampliadas com a variante "cálculo do/de salário líquido" — termo de maior volume da cauda (90,5K/mês) e mesma âncora do backlink real do `band.com.br` (F15), hoje sem cobertura no title/conteúdo. **Limite defensivo no link de compartilhamento (a pedido do Paulo, que perguntou pelo tamanho do `?d=` com listas grandes):** o campo de descrição livre do `itemList` não tinha limite de tamanho — colar um texto longo numa única lista já gerava um link de ~9 mil caracteres (medido). `ITEM_LIST_MAX_ITEMS` (20) e `ITEM_LIST_DESCRICAO_MAX_LENGTH` (60, com `maxLength` no input + espelhado no `.max()` do schema Zod — defesa em profundidade contra link adulterado) mantêm o pior caso (3 listas cheias) em ~6-7 mil caracteres, bem abaixo de limites práticos de URL/CDN. Novo teste cobrindo os dois limites. Validação: `pnpm typecheck` + `pnpm build` limpos, 39/39 e2e verdes (26 anteriores + 8 desktop novos + 5 mobile novos), 338/338 testes unitários do core. | `_pendente_` |
| 2026-08-05 | **0.7.0** | **Centralização de SEO — `buildMetadata()` + biblioteca `JsonLd.tsx` completa (F21).** Cada página tinha seu próprio objeto `Metadata` montado à mão (OpenGraph/Twitter/canonical repetidos e divergentes entre páginas). Novo `buildMetadata()` em `apps/web/src/lib/seo.ts` (mesmo padrão do Recibo Fácil, `frontend/src/lib/seo.ts`) centraliza title/description/keywords/canonical/OpenGraph/Twitter card num só lugar, com suporte a `canonical` alternativo (path ou URL absoluta) para o F23 (páginas programáticas de cauda longa) evitar canibalização entre páginas do mesmo cluster de busca. Adotado nas 7 páginas que tinham `Metadata` manual: `/calculadora/[slug]`, `/categoria/[categoria]`, `/categorias`, `/sobre`, `/contato`, `/politica-de-privacidade`, `/termos-de-uso` (o root `layout.tsx` manteve seu próprio objeto — tem campos únicos como `metadataBase`/`icons`/title com template). **`JsonLd.tsx` ganhou `HowToJsonLd`, `ItemListJsonLd` e `ArticleJsonLd`** (paridade com a biblioteca do Recibo Fácil): `HowToJsonLd` foi migrado de `lib/schema.ts`, onde vivia como objeto duplicado sem `@id`, para a biblioteca de componentes — o `BreadcrumbList` das 20 páginas de calculadora, que também era montado à mão sem `@id`, passou a usar o `BreadcrumbJsonLd` já existente (que já tinha `@id` desde a Sprint 1.2, só não estava sendo reaproveitado ali). `ItemListJsonLd` (novo) aplicado em `/categorias` (lista de 6 categorias) e `/categoria/[categoria]` (lista de calculadoras da categoria) — sinaliza ordem/total de itens ao Google, útil também pra GEO. `ArticleJsonLd` entra sem consumidor ainda (fecha a paridade da biblioteca antes do F22/blog precisar dela). As 6 páginas institucionais que não tinham **nenhum** JSON-LD (`sobre`, `contato`, `politica-de-privacidade`, `termos-de-uso`, `categoria`, `categorias`) ganharam `WebPage`+`BreadcrumbList` via `PageSeo`. `lib/schema.ts` foi reduzido a só construir os dados (breadcrumb items, passos do HowTo, FAQ com fallback) — a serialização do JSON-LD ficou 100% na biblioteca de componentes, uma única fonte de verdade pro `@id`/knowledge graph. Validação: `pnpm --filter web typecheck` limpo, `pnpm --filter web build` limpo (39 páginas SSG, `/calculadora/[slug]` continua `●`), 26/26 e2e verdes (`seo-metadata.spec.ts` confirma `WebApplication`/`BreadcrumbList`/`HowTo`/`FAQPage` intactos no JSON-LD). `next lint` com erro pré-existente de config (`eslint-import-resolver-typescript` ausente), não relacionado a esta entrega. | `_pendente_` |
| 2026-07-27 | **0.6.1** | **Fix crítico no 13º salário: 2ª parcela (e 1ª) usava o mês real do calendário em vez de 12 meses fixos.** Paulo reportou suspeita de bug nas parcelas do 13º ("especialmente a segunda"). Causa: `calcularMesesDireito` (`packages/core/src/trabalhista/decimo-terceiro.ts`), para quem já trabalhava antes do ano corrente (`mesAdmissao: null`, opção padrão do formulário), calculava o número de meses de direito das parcelas `'primeira'`/`'segunda'` como `Math.min(12, mesReferencia)` — e `mesReferencia` nunca é enviado pelo `DecimoTerceiroForm` (não existe esse campo na UI), caindo sempre no default `new Date().getMonth() + 1`, ou seja, o mês real do sistema no momento do cálculo. Calculando em julho (mês 7), a 2ª parcela considerava só 7/12 do 13º em vez de 12/12 — resultado errado e dependente da data em que se calculava (só coincidiria com o certo calculando em dezembro). A parcela `'total'` já tinha 12 meses hardcoded, por isso o sintoma mais visível era 1ª + 2ª não somarem o total. **Fix:** `mesesDireito` agora é sempre 12 para `mesAdmissao: null`, independente da parcela e da data do sistema (`mesReferencia` continua aceito/validado na API do core, só deixou de influenciar o cálculo). Quem foi admitido no ano corrente nunca teve esse bug. Suíte de teste reescrita (`decimo-terceiro.test.ts`, 24 testes): validação, `mesAdmissao` null com todas as parcelas em várias faixas salariais (cruzando faixas de INSS/IRRF), consistência 1ª+2ª=total, efeito de dependentes no IRRF, admissão no ano corrente (dias trabalhados, meses, borda em dezembro), e teste de regressão explícito simulando o sistema em janeiro/junho/julho/novembro/dezembro (`vi.setSystemTime`) confirmando que o resultado não muda mais com a data. Validação: `pnpm --filter @calculosonline/core test` (332/332), `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos. | `_pendente_` |
| 2026-07-27 | **0.6.0** | **`llms.txt` + GEO (F16).** Novo `apps/web/public/llms.txt`, mesmo padrão do Recibo Fácil (`frontend/public/llms.txt`): as 20 calculadoras listadas por categoria (trabalhista, impostos, financeiras, investimentos, saúde, negócios), modelo de negócio (100% gratuito, sem cadastro), diferenciais (cálculo 100% client-side — confirmado que nenhum form chama API, todos `'use client'` puro —, memória de cálculo sempre visível, compartilhamento de cálculo por link do F32) e FAQ curta. Promovido de P2 para P0 no backlog de aquisição de tráfego (25/07): o canal "AI Assistant" (ChatGPT/Copilot) do GA4 já traz ~17 sessões/mês sem nenhuma otimização de GEO feita, sinal de que a resposta por IA já cita o site. Sem métrica própria de validação (arquivo estático); acompanhar próximo export do GA4. | `_pendente_` |
| 2026-07-27 | **0.5.0** | **Compartilhamento de cálculo por link (WhatsApp), nas 20 calculadoras de uma vez.** Paulo propôs compartilhar o cálculo (não um PDF, como no Recibo Fácil) via query param `?d=` com os valores do formulário em base64 — abrir o link repreenche e recalcula sozinho. Decisões fechadas com ele: nome curto do param (`?d=`), botão "Compartilhar via WhatsApp" com mensagem incluindo o resultado formatado, e banner discreto "Valores de um cálculo compartilhado — edite e recalcule à vontade" ao abrir um link. Arquitetura aproveitou o que já existia centralizado: `FormProps` (`apps/web/.../forms/types.ts`) ganhou `sharedData`/`autoSubmit` e `onResult` passou a expor também o input validado (`data`) — mudança mecânica de 1-2 linhas nos 19 arquivos de form (mesmo padrão de toque em massa já usado nesta sessão pro default 0). `CalculatorForm` (`packages/ui`) ganhou `autoSubmit`: ao chegar `defaultValues` vindos de um link, faz `reset()` + dispara o cálculo sozinho, sem exigir clique. `CalculatorResult` ganhou o botão de compartilhar, remontando a mensagem do WhatsApp com o valor headline já formatado (reaproveita o `formatarValor` interno). Novo `apps/web/src/lib/shareLink.ts` (encode/decode base64url, sem libs externas). **Detalhe que quase passou despercebido:** `useSearchParams()` (necessário pra ler o `?d=`) exige um limite de `<Suspense>`, senão o Next tira a rota inteira do SSG — isolado num leaf invisível (`SharedDataReader`) só pra não perder o `generateStaticParams`/`revalidate: 86400` das 20 páginas (confirmado no build: `/calculadora/[slug]` continua `●` SSG, não virou `ƒ` dynamic). Evento de analytics `calculator_calculated` ganhou dimensão `origin` (`manual`/`shared_link`) e novo evento `calculator_shared`, pra não misturar cálculos reais com aberturas de link compartilhado nos números que já orientam o diagnóstico de GSC/GA4. Novo `share-link.spec.ts` (4 testes: link gerado, reabertura recalcula sozinho, ausência de param não mostra banner, param adulterado não quebra a página). Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 26/26 e2e verdes (22 anteriores + 4 novos). | `_pendente_` |
| 2026-07-26 | **0.4.3** | **Valor padrão 0 também em campos obrigatórios (reverte decisão da 0.4.0) + mês de admissão do 13º com nome do mês.** Paulo revisou prints de 2 calculadoras (13º e Rescisão) e apontou exatamente o problema que a 0.4.1 já tinha registrado como "sugestão de UX pendente": o placeholder cinza (ex. "3000" em Salário Bruto) parece um valor real preenchido, e ao tentar calcular sem editar o campo o erro genérico "Required" (do zod, em inglês) reforça a confusão. A decisão da 0.4.0 de deixar campos obrigatórios em branco "de propósito" foi revertida a pedido dele: todo campo numérico obrigatório sem `.default()` (Salário Bruto, Peso, Altura, Valor do Empréstimo, Custo Total etc. — 24 campos em 16 formulários) ganhou `.default(0)` no próprio schema Zod, reaproveitando o `extractSchemaDefaults()` já existente (`packages/ui/src/CalculatorForm/index.tsx`, F28/0.4.0) — o placeholder de exemplo virou código morto e foi removido de todos eles. Efeito colateral desejado: submeter sem editar agora cai na mensagem real do schema em português (ex. "Salário deve ser positivo") em vez do "Required" genérico em inglês. Campos com `min(0)` (não `positive()`, ex. Saldo do FGTS, Rendimentos Tributáveis) aceitam 0 como valor legítimo — Paulo confirmou esse comportamento ao pedir o mesmo default para "Saldo do FGTS". **Mês de admissão** (`DecimoTerceiroForm`) trocado de input numérico cru para `<select>` com os 12 meses nomeados (Janeiro–Dezembro) + opção "0 — Já trabalhava antes do ano corrente"; como é o primeiro `<select>` do projeto ligado a um campo `z.number()` (os demais são enums de string), `CalculatorForm` ganhou detecção do tipo base do campo (desembrulhando `ZodDefault`/`ZodOptional`) para aplicar `setValueAs: Number` só quando o schema é numérico. 1 teste e2e ajustado (`quick-add-chips.spec.ts` — Salário Bruto não começa mais vazio). Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 22/22 e2e verdes; conferido também no HTML de produção (`pnpm start`) que o `<select>` de mês lista as 13 opções corretamente. | `_pendente_` |
| 2026-07-26 | **0.4.2** | **Fix: `pnpm dev` caindo sozinho depois de ~1min30 (`turbo.json`).** Paulo reportou navbar e FAQ "não funcionando" — a causa real não era nenhum componente: `turbo.json` estava com `"ui": "tui"` (modo de terminal interativo do Turborepo 2.x), que tem bug conhecido de encerrar tarefas persistentes (`dev`, marcado `persistent: true`) sem avisar em certos terminais. Quando o servidor morre no meio da sessão, qualquer interação para de responder (parece bug de UI, mas é ausência de servidor). Como hoje só `web` tem script `dev` (`core`/`ui` não têm), o Turbo não coordena nada em paralelo aqui — o TUI era overhead puro, sem benefício. Fix: `"ui": "stream"` (modo clássico de log sequencial, sem essa instabilidade). Validado rodando `pnpm dev` e confirmando o processo vivo por 100s+ (antes morria por volta de 90s) — navbar (dropdowns `Menu` do Headless UI) e FAQ (accordion da home) testados via Playwright no servidor saudável, ambos funcionam normalmente; a causa nunca foi o código deles. Achado à parte, corrigido junto: um edit anterior desta própria sessão tinha apagado sem querer a linha de cabeçalho desta tabela do CHANGELOG — restaurada. Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 22/22 e2e verdes. | `_pendente_` |
| 2026-07-26 | **0.4.1** | **Detalhamento do cálculo sempre visível + sinal (+)/(−) em cada linha.** Paulo notou que o detalhamento ficava escondido atrás de um "Ver detalhamento do cálculo" que precisava de clique, e que as linhas só diferenciavam crédito/débito por cor (verde/vermelho) — problema de transparência e de acessibilidade (sinal só por cor). `CalculatorResult` (`packages/ui/src/CalculatorResult/index.tsx`): removido o `useState`/botão de toggle — a lista de detalhamento agora renderiza sempre aberta, sem interação necessária; nova função `sinalNatureza()` prefixa cada linha com "+ " (crédito, ex. Salário Bruto) ou "− " (débito, ex. INSS/IRRF) conforme o mesmo campo `tipo` que já definia a cor — linhas neutras (ex. o próprio resultado final) continuam sem prefixo. Também confirmado nesta sessão que o "3000" que aparecia no campo Salário Bruto era só placeholder (texto cinza), não um valor real preenchido — gerou confusão do Paulo ao ler print, registrado como sugestão de UX pendente (placeholder pode ser confundido com valor real). Ajustado 1 teste e2e que clicava no botão removido (`calculadora-margem-lucro.spec.ts`). Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 22/22 e2e verdes (2ª tentativa — 1ª veio contaminada por processos `next dev` anteriores mortos com `kill -9`/`fuser -k` deixando `.next` num estado inconsistente, mesma classe de problema já visto ontem; resolvido limpando `.next` e evitando kills abruptos concorrentes). | `_pendente_` |
| 2026-07-25 | **0.4.0** | **Valores padrão automáticos nos formulários + limpeza da navbar.** Paulo notou que o preenchimento das 20 calculadoras não é amigável — nenhum campo tem valor padrão visível (nem 0), obrigando o usuário a adivinhar se pode deixar em branco. Auditoria achou a causa: os 19 formulários (`apps/web/src/components/calculadoras/forms/`) já declaram `.default(0)`/`.default('nao')`/`.default('moderado')` etc. nos schemas Zod, mas esse default só era aplicado no cálculo pós-envio — nenhum form passava `defaultValues` pro `CalculatorForm`, então o campo ficava visualmente vazio até o usuário digitar. **Fix centralizado** (não em cada form): `CalculatorForm` (`packages/ui/src/CalculatorForm/index.tsx`) ganhou `extractSchemaDefaults()`, que lê os `.default()` já declarados no próprio schema via introspecção (`_def.typeName === 'ZodDefault'`) e pré-popula o formulário — uma única fonte de verdade (o schema), sem duplicar valores em cada um dos 19 forms. Campos obrigatórios (sem `.default()`, ex. Salário Bruto, Peso, Valor do Empréstimo) continuam em branco de propósito — não fazia sentido simular um valor real do usuário. Verificado via Playwright num script ad-hoc (headless, sem `chromium-cli` disponível no ambiente): campos opcionais renderizam com o valor/opção correta pré-selecionada (ex. "Mês de admissão" = 0, "Parcela" = "Total (1ª + 2ª)"), campo obrigatório continua vazio só com placeholder. **Navbar simplificada:** removidos os links soltos "Categorias" (duplicava o botão destacado já existente ao lado), "Blog", "Sobre" e "Contato" do menu superior (`Navigation.tsx`, array `simpleLinks` removido) — os 3 últimos continuam acessíveis pelo rodapé (`Footer.tsx`), mesma decisão de reduzir poluição visual do header já tomada no Recibo Fácil. Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 22/22 e2e verdes (achado à parte: a 1ª rodada de e2e após o `pnpm build` veio com 18 falhas — página renderizando sem CSS nenhum — reincidência do cache `.next` corrompido por misturar `build`/`dev` já documentado em `AGENTS.md`; resolvido apagando `apps/web/.next` antes de rodar `test:e2e`, nada a ver com o código desta entrada). | `_pendente_` |
| 2026-07-25 | **0.3.2** | **Retargeting de SEO da calculadora de Décimo Terceiro (13º) — mesmo padrão do "recibo-simples" no Recibo Fácil.** Antes de comprar um backlink de autoridade para essa página (candidata prioritária do backlog de aquisição), auditoria de conteúdo achou um descasamento real: title, H1, corpo e FAQ usavam só "13º salário", nunca "décimo terceiro" por extenso — mas a pesquisa de palavra-chave (Keyword Surfer) mostrou que o termo de maior volume do cluster é exatamente "decimo terceiro salario" (550.000 buscas/mês BR), com "calculadora/calcular decimo terceiro" (135.000/mês cada) logo atrás — o Google tinha pouquíssimo sinal textual pra casar a página com a busca de maior volume. Mesmo diagnóstico e fix que o Recibo Fácil aplicou em `/recibo-simples` (consolidação "recibo simples" × "recibo de pagamento", 2026-06-25): `tituloLongo` do registry (`apps/web/src/lib/calculators.ts`) retargetado para "Calculadora de Décimo Terceiro Salário (13º)" (title final passa a conter as duas variantes de maior volume), `palavrasChave` ampliado; H1 e parágrafo de abertura do MDX (`content/calculadoras/decimo-terceiro.mdx`) reescritos para introduzir "décimo terceiro salário" logo na primeira frase; 2 novas perguntas na FAQ ("Décimo terceiro salário e 13º salário são a mesma coisa?", "Como calcular o décimo terceiro salário proporcional?") — entram automaticamente no schema `FAQPage` via `lib/faq.ts`, sem código novo. `titulo` curto (usado em cards/breadcrumb/nav) mantido "13º Salário" de propósito, para não confundir usuários recorrentes. Validação: `pnpm --filter web typecheck` limpo. | `_pendente_` |
| 2026-07-25 | **0.3.1** | **Ativação do Microsoft Clarity em produção + alinhamento com o padrão do Recibo Fácil.** O componente `MicrosoftClarity.tsx` (`apps/web/src/components/analytics/`) já existia desde a Sprint 1.4.1, mas nunca tinha recebido um Project ID real — o script nunca chegava a carregar em produção (confirmado via `curl` no HTML de produção, sem o script `clarity.ms/tag/*`). Paulo forneceu o ID real do projeto (`xs93eywu1y`), configurado em `apps/web/.env.local` (uso local) e pendente de configuração em produção no Vercel (`NEXT_PUBLIC_CLARITY_PROJECT_ID`). Comparado com a implementação do Recibo Fácil (mesmo autor, referência) e corrigidas 2 diferenças: **estratégia de carregamento** trocada de `afterInteractive` para `lazyOnload` (mesma decisão do Recibo Fácil, evita ~80-130ms de bloqueio da main thread medido no Lighthouse, melhora TBT/INP no mobile); **preconnect** `<link rel="preconnect" href="https://www.clarity.ms" />` adicionado ao `<head>` de `apps/web/src/app/layout.tsx` (que não tinha `<head>` manual antes). Decisão deliberada de **não** replicar o fallback de ID hardcoded no código que o Recibo Fácil tem — o padrão calculosonline (só renderiza com env var presente, igual ao `GoogleAnalytics`) evita ID de tracking commitado no repositório. Validação: `pnpm --filter web typecheck` limpo. | `_pendente_` |
| 2026-07-20 | **0.3.0** | **Fix do lastmod do sitemap + schema HowTo + chips de valor rápido (padrão Recibo Fácil).** Paulo notou em produção que o `lastmod` das calculadoras no sitemap ainda mostrava datas antigas mesmo após a entrega de 0.1.0/0.2.0 — causa: `app/sitemap.ts` usava `calc.dataAtualizacao` puro (data de revisão das tabelas legislativas, fixa em `2026-01-01` para as 20 calculadoras, também exibida ao usuário como selo de confiança) como único sinal de freshness, então uma mudança de SEO (title/FAQ) sem alteração das tabelas não movia o `lastmod`. Fix: nova constante `seoRefreshDate` (bumpada manualmente a cada mudança de SEO relevante, mesmo padrão do recibofacil) — o `lastmod` de cada calculadora agora usa a mais recente entre `dataAtualizacao` e `seoRefreshDate`, sem alterar o que é mostrado ao usuário como data de revisão legislativa. **Schema HowTo** adicionado em `apps/web/src/lib/schema.ts` (3 passos genéricos por calculadora: preencher, calcular, ver detalhamento) — fecha a lacuna do plano de negócios (seção 3.1) que pedia `HowTo`/`FAQPage` para rich snippets e nunca tinha sido implementado; `seo-metadata.spec.ts` atualizado para cobrir. **Chips de valor rápido** — portado o `CurrencyInputWithQuickAdd` do recibofacil (mesmas classes Tailwind: chip cinza "Zerar" + chips azuis "+N") como opção `quickAdd` em `FieldMeta` do `CalculatorForm` compartilhado (`packages/ui`): "Zerar" zera o campo, cada chip **soma** ao valor atual (não substitui, arredondado a 2 casas). Aplicado no campo de valor principal de 16 formulários / 17 calculadoras com campo R$ (todas exceto IMC/Calorias/Porcentagem, que não têm campo monetário), com 3 presets por escala (`QUICK_ADD_SALARIO`, `QUICK_ADD_INVESTIMENTO`, `QUICK_ADD_VALOR_GRANDE` em `apps/web/src/lib/quickAddPresets.ts`). Novo `quick-add-chips.spec.ts` (5 testes: soma cumulativa, zerar, soma a partir de vazio, campo sem `quickAdd` não ganha chips, preset de investimento). Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 22/22 e2e verdes (17 anteriores + 5 novos). | `_pendente_` |
| 2026-07-19 | **0.2.0** | **Playwright E2E + sitemap/robots nativos (padrão Recibo Fácil) + fix crítico de validação.** `apps/web/playwright.config.ts` + `tests/e2e/` (mesmo padrão do Recibo Fácil: `testDir`, reaproveita dev server, projeto único `chromium`): `calculadora-margem-lucro.spec.ts` (fluxo completo do form ao resultado, referência para as outras 19 calculadoras), `seo-metadata.spec.ts`, `sitemap-robots.spec.ts`, `categorias-navegacao.spec.ts` — 17 testes. `next-sitemap` removido (dependência, config, script `postbuild`, `public/sitemap.xml`/`public/robots.txt` estáticos) e substituído por `app/sitemap.ts`/`app/robots.ts` nativos do Next, no mesmo padrão do Recibo Fácil: `lastModified` real por calculadora (`calc.dataAtualizacao` do registry, não data de build) e prioridade maior (0.95) para calculadoras `featured`; corrige também um bug do sitemap antigo, que listava `/privacidade` (rota que só redireciona) em vez da canônica `/politica-de-privacidade`. **Bug crítico encontrado pelo próprio teste novo, não estava no escopo original:** campo numérico opcional deixado em branco virava `NaN` via `valueAsNumber` do react-hook-form, e o `.default(0)` do Zod só resgata `undefined`, nunca `NaN` — travando a submissão com "Expected number, received nan". Não era específico de uma calculadora: quase todas as 20 têm campos opcionais nesse padrão (dependentes de IRRF, aporte mensal, descontos, dias de falta etc.). Corrigido na raiz, em `packages/ui/src/CalculatorForm/index.tsx` (troca de `valueAsNumber` por `setValueAs` com tratamento explícito de string vazia), valendo para todas as calculadoras de uma vez. Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 322 testes unitários do core verdes, 17/17 e2e verdes. | `964bde0` |
| 2026-07-19 | **0.1.0** | **Correções de SEO (diagnóstico Search Console).** Export do GSC (`gsc/`, últimos 3 meses até 17/07) mostrou ~1.120 impressões e apenas 2 cliques — CTR ~0% mesmo em páginas já na 1ª página (`margem-lucro` pos. 9.6, `porcentagem` pos. 9.1, `das-mei` pos. 10.3). Causa raiz: title genérico repetido em todas as calculadoras e `FAQPage` com as mesmas 3 perguntas em todas as páginas. **Title por calculadora:** `buildCalculatorTitle()` (`apps/web/src/lib/seo.ts`) substitui o padrão fixo `"... Online e Gratuita 2026"` por `"... 2026 — Grátis, sem Cadastro"`; corrige de quebra um bug real em `inss`/`irpf`/`das-mei` (já tinham "2026" no `tituloLongo`, duplicando o ano no title final). **FAQPage real:** `apps/web/src/lib/faq.ts` (novo) extrai as perguntas/respostas reais da seção "Perguntas frequentes" de cada MDX editorial (101 perguntas nas 20 calculadoras) e alimenta o schema em `lib/schema.ts`, no lugar das 3 perguntas genéricas fixas; fallback genérico mantido só para calculadora futura sem MDX. | `964bde0` |
