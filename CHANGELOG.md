# Changelog — Calculos Online

Funcionalidades entregues em produção. Versionamento SemVer (`Major.Minor.Patch`)
do app `web` (`apps/web/package.json`): `minor` para nova funcionalidade, `patch`
para ajuste/correção, `major` para marco de estabilização. Histórico anterior à
estabilização fica em `0.x`.

- **Stack atual:** Next.js `15.5.18` (React `19.0.0`), monorepo Turborepo com
  `packages/core` (cálculos) e `packages/ui` (componentes) versionados à parte.
- **Coluna Versão:** versão de `apps/web` *após* aquela entrada.
- Entradas mais recentes no topo. Datas em `AAAA-MM-DD`. `Commit` = commit principal.

| Data | Versão | Funcionalidade | Commit |
|------|--------|-----------------|--------|
| 2026-07-19 | **0.2.0** | **Playwright E2E + sitemap/robots nativos (padrão Recibo Fácil) + fix crítico de validação.** `apps/web/playwright.config.ts` + `tests/e2e/` (mesmo padrão do Recibo Fácil: `testDir`, reaproveita dev server, projeto único `chromium`): `calculadora-margem-lucro.spec.ts` (fluxo completo do form ao resultado, referência para as outras 19 calculadoras), `seo-metadata.spec.ts`, `sitemap-robots.spec.ts`, `categorias-navegacao.spec.ts` — 17 testes. `next-sitemap` removido (dependência, config, script `postbuild`, `public/sitemap.xml`/`public/robots.txt` estáticos) e substituído por `app/sitemap.ts`/`app/robots.ts` nativos do Next, no mesmo padrão do Recibo Fácil: `lastModified` real por calculadora (`calc.dataAtualizacao` do registry, não data de build) e prioridade maior (0.95) para calculadoras `featured`; corrige também um bug do sitemap antigo, que listava `/privacidade` (rota que só redireciona) em vez da canônica `/politica-de-privacidade`. **Bug crítico encontrado pelo próprio teste novo, não estava no escopo original:** campo numérico opcional deixado em branco virava `NaN` via `valueAsNumber` do react-hook-form, e o `.default(0)` do Zod só resgata `undefined`, nunca `NaN` — travando a submissão com "Expected number, received nan". Não era específico de uma calculadora: quase todas as 20 têm campos opcionais nesse padrão (dependentes de IRRF, aporte mensal, descontos, dias de falta etc.). Corrigido na raiz, em `packages/ui/src/CalculatorForm/index.tsx` (troca de `valueAsNumber` por `setValueAs` com tratamento explícito de string vazia), valendo para todas as calculadoras de uma vez. Validação: `pnpm typecheck` + `pnpm lint` + `pnpm build` limpos, 322 testes unitários do core verdes, 17/17 e2e verdes. | `_pendente_` |
| 2026-07-19 | **0.1.0** | **Correções de SEO (diagnóstico Search Console).** Export do GSC (`gsc/`, últimos 3 meses até 17/07) mostrou ~1.120 impressões e apenas 2 cliques — CTR ~0% mesmo em páginas já na 1ª página (`margem-lucro` pos. 9.6, `porcentagem` pos. 9.1, `das-mei` pos. 10.3). Causa raiz: title genérico repetido em todas as calculadoras e `FAQPage` com as mesmas 3 perguntas em todas as páginas. **Title por calculadora:** `buildCalculatorTitle()` (`apps/web/src/lib/seo.ts`) substitui o padrão fixo `"... Online e Gratuita 2026"` por `"... 2026 — Grátis, sem Cadastro"`; corrige de quebra um bug real em `inss`/`irpf`/`das-mei` (já tinham "2026" no `tituloLongo`, duplicando o ano no title final). **FAQPage real:** `apps/web/src/lib/faq.ts` (novo) extrai as perguntas/respostas reais da seção "Perguntas frequentes" de cada MDX editorial (101 perguntas nas 20 calculadoras) e alimenta o schema em `lib/schema.ts`, no lugar das 3 perguntas genéricas fixas; fallback genérico mantido só para calculadora futura sem MDX. | `_pendente_` |
