# apps/sheets-plugin — Google Apps Script

Placeholder. Implementação na **Sprint 2.3** (Plugin Google Sheets).

Stack prevista:
- `clasp` CLI para sincronizar com o Apps Script
- Bundle do `@calculosonline/core` para Apps Script via webpack
- Funções customizadas: `=CALCRESCISAO(...)`, `=CALCINSS(...)`, etc.
- Sidebar HTML reutilizando componentes do `@calculosonline/ui`
- Publicação no Google Workspace Marketplace
