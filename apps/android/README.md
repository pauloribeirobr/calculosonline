# apps/android — TWA (Trusted Web Activity)

Placeholder. Implementação na **Sprint 1.5** (PWA + Android).

Stack prevista:
- `bubblewrap` CLI para gerar o wrapper TWA a partir do PWA em `apps/web`
- `twa-manifest.json` apontando para `https://calculosonline.com.br`
- Assinatura digital (keystore) gerada localmente
- `/.well-known/assetlinks.json` servido pelo `apps/web/public/`
- Build APK/AAB → upload no Google Play Console

Custo: R$ 130 (one-time) na conta Google Play.
